#pragma once

// All runtime settings have moved to openchaos/config.json ("openchaos" section).
// See engine/io/oc_config.h for the API and new_game_devlog/config_json/README.md for docs.
//
// debug_config.h (OC_DEBUG_* flags) is still a compile-time header — see debug_config.h.
