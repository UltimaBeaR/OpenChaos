// MAP_light_map is defined in map.cpp (not here) because it has a non-trivial initializer
// that references functions in map.cpp. This file exists for consistency with the _globals pattern.
#include "map/map_globals.h"
