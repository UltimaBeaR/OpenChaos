# Этап 12 — Релиз 1.0

**Цель:** довести игру до состояния релиза 1.0 за ~неделю работы.

**Подход:** минимально-достаточный 1.0 — не все шероховатости фиксятся, не весь техдолг закрывается. Главное чтобы для конечного пользователя выглядело как адекватный релиз. Допустимые трейдофы и мелкие баги — да; блокеры геймплея и явные регрессии vs релиза — нет.

Всё что не вошло в этот файл — в [`known_issues_and_bugs.md`](../known_issues_and_bugs/known_issues_and_bugs.md) (баги) и/или будет обработано в более поздних этапах. Активный список багов: [`known_issues_and_bugs.md`](../known_issues_and_bugs/known_issues_and_bugs.md).

---

> **Скоп 1.0 одной фразой.** Самые крупные доработки в этом релизе:
> 1. **Переработка управления** (геймпад + клавиатура + опции) — главная фича-задача. ✅ **Сделано** (см. issue «Переработка управления» в [архиве решённых](../known_issues_and_bugs/known_issues_and_bugs_resolved.md)).
> 2. **Доработки главного меню и конфигурации проекта** — см. Задачу #7 ниже + связанные issue в 1.0 (gamma, language, missing-resources, etc).
> 3. ~~**Дистрибуция** (установщики на все платформы, иконка, mod-like install)~~ — **улучшения отложены на post-1.0** (см. Задачу #8 + [`stage13.md`](stage13.md) «Инфраструктура и дистрибуция»). Для 1.0 — текущие zip-пакеты + готовая Windows-иконка.
>
> Всё остальное — **по большей части багфиксы** (визуал, краши, мелкие геймплейные регрессии, UX-полировка).
>
> **Текущий статус (2026-06-08).** Главные фичи закрыты; из крупного по сути осталось:
> 1. **Документация** — перевод важных публичных доков (`GAMEPLAY_CHANGES.md`) + актуализация README перед релизом (см. «Непосредственно перед релизом» #1, #3).
>
> Плюс смоук-тест прохождения перед релизом. (Дистрибуция для 1.0 фиксируется в текущем виде — улучшения в stage13.)

---

## Принципы стабильности 1.0

Критерий **не** «ноль ASSERT'ов на любых сценариях» — это нереалистично к 1.0 (полное hardening вынесено в post-1.0, см. запись «Все ASSERT'ы молчат на любых сценариях»). Критерий 1.0 — **в нормальной игре ничего не крашится**; редкие edge-case ASSERT'ы (как ASSERT при подтягивании на ящик, выпадающий очень редко) приемлемы, если игрок не «лезет в залупу намеренно». Поведение «крашить на ASSERT» **сохраняется** — лучше явный краш с логом чем тихая порча памяти / undefined behavior. Все ASSERT-краши пишутся в `OpenChaos.crash_log.txt`.

---

## Задачи

### 1. Пофиксить все issue из списка [`known_issues_and_bugs.md`](../known_issues_and_bugs/known_issues_and_bugs.md)
Основной массив работы. Содержит ~24 issue: крэши/зависания, визуальные баги, переработка управления (главная фича-задача), геймплейные баги, PieroZ-фикс, настройки/конфиг, debug-cleanup. Закрывать в любом удобном порядке; крупные блокеры — раньше.

### 2. Тестирование смен контроллеров (на Windows) — ✅ сделано
**Статус: сделано (2026-06-08).** В процессе обычной игры (не целенаправленно, а по ходу тестирования других задач) проблем со сменой контроллеров не выявлено. Известные баги этого класса закрыты отдельно (см. resolved: «R3 на свежеподключённом DualSense», «R3 + popup» — оба не воспроизвелись).

~~Прогнать сценарии переключения между **DualSense ↔ Xbox ↔ клавиатура** в разных ситуациях (геймплей, меню, паузы, mid-mission подключение/отключение, alt-tab) — отловить рассинхрон input state. **Только Windows** — на macOS геймпад работает базово; на Steam Deck геймпады не используются (встроенный контроллер).~~

### 3. Единая обработка закрытия окна (X) — ✅ сделано
**Статус: сделано.** Кейс «Outro не закрывается крестиком» закрыт (перенесён в resolved).

Глянуть в целом как сейчас обрабатывается клик в крестик окна в разных состояниях игры (главное меню / outro / геймплей / pause / loading). Возможно сделать единое поведение — например, **мгновенный выход** при X независимо от состояния (как стандартный desktop-app паттерн). Не сложно, но даёт много пользы для UX/полированности. **Частный кейс** — issue «Outro не закрывается крестиком окна» в [`known_issues_and_bugs.md`](../known_issues_and_bugs/known_issues_and_bugs.md): закрывается этой задачей автоматически (нет смысла фиксить отдельно).

### 4. GPU wake-up на alt-tab и после загрузки уровня — ❌ решили НЕ делать для 1.0
**Статус: решили НЕ делать для 1.0, перенесено в post-1.0 (2026-06-08).** После ресёрча: ресайз/alt-tab сами по себе ненадёжны (в части случаев не помогают, спасает только рестарт), а **чистого кросс-вендорного in-app способа форсить высокие частоты GPU не существует** — все реальные рычаги драйверно/ОС/пользовательские. Подробный разбор находок и вариантов — в записи «Спорадические катастрофические просадки FPS» в [`known_issues_and_bugs.md`](../known_issues_and_bugs/known_issues_and_bugs.md). Для 1.0 — принимаем как есть.

~~При катастрофических просадках FPS (см. resolved/active записи про «спорадические GPU power-state просадки») найден костыль — действие, которое делается при изменении разрешения окна, **выбивает GPU обратно в высокие частоты**. Применить тот же приём автоматически: (а) **при каждом alt-tab (раскрытие окна из фона)** — пробуждать GPU; (б) **после каждой загрузки уровня** — тоже. Низкоприоритетный по природе фикс (на корневую причину power-gating мы не влияем), но снимает практический симптом без расследования драйверов.~~

### 5. Автопауза при скрытии окна (alt-tab / минимизация в трей) — ❌ решили не делать
**Статус: решили НЕ делать (2026-06-08).** Сознательно отказались от автопаузы при уходе окна в фон.

~~Если игра в режиме геймплея и окно уходит в фон (alt-tab, свернуть в трей) — автоматически ставить на паузу. На возврат фокуса игра остаётся на паузе, игрок снимает её сам.~~

### 6. Настройка FPS лимита (с выключением / unlimited) — ✅ сделано
**Статус: сделано (2026-06-08).** Настройка максимального FPS (с unlimited) реализована. Подробности — в записи про систему конфига (см. resolved «Настройки и конфигурация»).

~~Добавить настройку «максимальный FPS» — значения (60 / 120 / 144 / 240 / Unlimited) или произвольное число. Хранить как минимум в `config.json`.~~

### 7. Доработка главного меню игры
Добить все настройки которые должны быть доступны пользователю — должны быть в меню опций. **Отдельный открытый вопрос — настройка управления:** сейчас можно настраивать только клавиатуру, и уже есть расхождения между настройками и реальной работой; контроллер не настраивается вообще. Вместе с большой переработкой управления (см. issue «Переработка управления» в 1.0) нужно решить **что делать с настройкой управления**: (1) сделать полноценную кастомную систему настройки — но это долго; (2) убрать настройку из меню (оставить только в конфиге); (3) убрать вообще, не давать пользователю настраивать; (4) сделать в упрощённом виде. Текущая дефолтная система настройки скорее всего не вытягивает — возможно стоит её **не переиспользовать** а сделать полностью кастомную, либо отказаться от настройки в 1.0. **Порядок:** сначала закрыть большую задачу по переработке управления, затем сразу решить и доделать вопрос настройки (любым из вариантов) — нельзя оставлять "настройки есть но они не совпадают с реальностью" к релизу.

**✅ Решено (для настройки управления):** выбран вариант (3) — настройку управления убрали полностью (переназначения нет, биндинги фиксированы), чтобы не было расхождения «настройки есть, но не совпадают с реальностью». Вместо неё в меню паузы добавлен экран **help** — схема управления (под активное устройство) + описание механик. Переназначение клавиш вернётся post-1.0 на новой системе биндингов. (Остальные настройки меню — gamma/language/FPS и пр. — отдельными записями.)

### 8. Удобная установка / дистрибуция — ❌ улучшения отложены, для 1.0 как есть
**Статус (2026-06-08): для 1.0 дистрибуция остаётся в текущем виде, все улучшения перенесены в [`stage13.md`](stage13.md) → «Инфраструктура и дистрибуция».**

Что есть для 1.0 (не меняем):
- **Per-platform zip-пакеты** (`release/` — Windows/macOS/Linux). Игрок распаковывает рядом с ресурсами оригинала вручную.
- ✅ **Windows-иконка вшита в exe** — сделана в 1.0 (своя иконка окна). Детали и мастера-исходники — в stage13.

Что отложено в stage13 (НЕ в 1.0): полноценные установщики на все платформы с авто-поиском папки оригинала (NSIS/Inno, macOS `.dmg`/`.app`, Steam Deck/Linux), Steam-дистрибуция (свой app/мод или beta-ветка у правообладателя), иконки на macOS/Linux/Steam Deck.

---

## Непосредственно перед релизом

Делается **в самом конце 1.0**, после того как все задачи выше закрыты — раньше нет смысла (проект ещё меняется и любая ранняя версия этих работ устареет). Порядок важен.

### 1. Ноль русского языка в репозитории (перевод всей документации и кода на английский)
**Решение (2026-06-08): ПОЛНЫЙ перевод всего репозитория к 1.0 НЕ делаем** (большой объём, не блокер релиза — переносится на post-1.0). **НО к 1.0 обязательно:** (1) перевести на английский **некоторые важные публичные документы** — в первую очередь `GAMEPLAY_CHANGES.md` (его читает игрок/контрибьютор); (2) в целом **доработать и обновить документацию перед релизом** — это уже отражено в пункте «3. Актуализация README» ниже. Остальной массив (внутренние планы, devlog, KB, CLAUDE.md, скиллы, комментарии в коде) — переводим post-1.0.

Полный объём (на post-1.0): перевести **всю** документацию на английский: `new_game_planning/`, `new_game_devlog/`, `original_game_knowledge_base/`, `original_game_knowledge_base_dense/`, заметки и т.п. — **в том числе все инструкции для Claude**: корневой и любые вложенные `CLAUDE.md`, все скиллы в `.claude/skills/` (SKILL.md + сопутствующие файлы), агентские/инструкционные файлы. Также **любой** русский текст в исходниках: комментарии в коде (`new_game/`, `libDualsense/` и пр.), строковые литералы, TODO/FIXME, debug-вывод, имена в заметках.

⚠️ Уточнение по строковым литералам: переводим только разработческий/технический текст (комментарии, debug-вывод, лог-сообщения, internal-имена). Если строковый литерал — это **намеренно русский игровой/пользовательский текст** — его **НЕ трогать**, остаётся на русском.

⚠️ При переводе `CLAUDE.md` обновить и секцию **«Язык»** (правило «документация на русском» к 1.0 меняется на английский).

Критерий готовности: grep по кириллице `[А-Яа-яЁё]` по всему репо (исключая read-only `original_game/` и бинарные ресурсы) → пусто.

**Причина:** открытый репозиторий — русский текст это барьер для контрибьюции/PR-ов.

Делать через subagent'ов в конце 1.0.

### 2. Полный аудит изменений относительно оригинала (большая задача, агентом)
**Решение (2026-06-08): к 1.0 НЕ делаем** (большая задача, не блокер релиза). Возможно сделаем **после 1.0**.

Пройтись по всему git-истории и собрать список реальных отличий нашей версии от оригинала. **Две сводки, раздельно:**
- (а) **геймплейные изменения** — всё что игрок видит/ощущает (фиксы багов, новое поведение, доработки). Часть уже есть в `GAMEPLAY_CHANGES.md` но не всё.
- (б) **крупные технические изменения** — переход с D3D6 на OpenGL, x86→x64, добавление системы скелетной анимации, изменение системы сортировки полупрозрачных, пересмотр отражений в лужах, camera_improvements (wall collision), CRT-шейдер, fps unlock + render interpolation, кастомное меню опций, libDualsense, и т.д.

**Эталон сравнения:** «предполагаемая релизная версия» = пре-релизные исходники почти как retail PC (за исключением некоторых визуальных моментов). Делать через subagent'а, не в основном контексте. Результат — публичный документ к релизу.

### 3. Актуализация README (`README.md` в корне)
Перетряхнуть README целиком, привести в актуальное состояние, в том числе вписать (в подходящем виде) найденные при «Полном аудите изменений vs оригинала» отличия — то что должен знать потенциальный игрок и контрибьютор. Публичное лицо проекта — должно отражать актуальное состояние к 1.0.

### 4. Смоук-тест прохождения игры (на Windows)
Быстрое прохождение игры с читами от начала до конца — удостовериться что нет блокеров прохождения (краши, застревания, неработающие триггеры). Пользователь игру ещё не проходил от начала до конца. Всё что выловится в смоук-тесте → фиксим в 1.0; остальные мелкие баги — post-1.0. **Только Windows** — на macOS/Steam Deck полноценное прохождение не делаем (см. post-1.0 запись «Полноценное мультиплатформенное тестирование»).

---

Активный список багов: [known_issues_and_bugs.md](../known_issues_and_bugs/known_issues_and_bugs.md)
Архив решённых: [known_issues_and_bugs_resolved.md](../known_issues_and_bugs/known_issues_and_bugs_resolved.md)
