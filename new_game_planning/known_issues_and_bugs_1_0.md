# Известные проблемы и баги — 1.0

Активный список багов и доработок которые **нужно сделать до релиза 1.0**.

- Всё что отложено на потом → [`known_issues_and_bugs_post_1_0.md`](known_issues_and_bugs_post_1_0.md)
- Исправленные и закрытые → [`known_issues_and_bugs_resolved.md`](known_issues_and_bugs_resolved.md) (общий архив для 1.0 и post-1.0)
- Пофикшенные баги пре-релиза → [`prerelease_fixes.md`](prerelease_fixes.md)
- План релиза 1.0 → [`stage12.md`](stage12.md)

**Правило переноса:** баг исправлен / закрыт / won't-do → ПЕРЕНОСИТЬ (вырезать и вставить) в `known_issues_and_bugs_resolved.md` в соответствующую `## … (исправленные)` секцию. Не оставлять в активном файле, не ограничиваться сменой статуса.

---

## Крэши / зависания

| Проблема | Описание | Статус |
|----------|----------|--------|
| Зависание на спецэкране «слишком много убитых цивилов» (RTA) | **Репро (2026-05-17, RTA):** пройти миссию RTA успешно, но **убив много мирных жителей**. После «Mission Completed» вылезает спецэкран-нотация с картинкой копа и текстом начинающимся на *"I'm at the end of my patience"* (выговор за убийства цивилов). Этот экран **зависает**: картинка статична, ни одна кнопка/клавиша не реагирует, **видеокарта при этом нагружена** (слышно по кулеру) — похоже на экране **нет FPS-лока** (в главном меню должен быть 300, тут, видимо, не применяется → render loop крутится без ограничения). Workaround в сессии пользователя: свернуть/развернуть окно и кликнуть мышью в окно → картинка сменилась на следующую, но **продолжило виснуть** так же. Похоже на класс багов «застрявший фуллскрин-скрин» (экран не отдаёт управление, ср. с пофикшенным «briefing поверх loading screen» и плавающим «brief-скрин на экране сохранения»). Нужно: найти state-машину этого notation-экрана, проверить (1) почему не реагирует ввод (не вызывается advance/skip handler), (2) почему не применяется frame cap (FPS-лок не выставлен для этого экрана). **Если не получится воспроизвести — снести в post-1.0.** | Исследовать |

---

## Визуальные проблемы

| Проблема | Описание | Статус |
|----------|----------|--------|
| Экран брифинга не скрывается при загрузке миссии (вместо loading screen) | При загрузке миссии briefing screen остаётся нарисованным поверх — а loading screen **рисуется сзади и не виден**. В retail виден правильный loading screen при загрузке миссии, не briefing. В D3D-версии PieroZ этого бага нет — значит регрессия появилась при переходе на OpenGL или позже. **Воспроизводится стабильно**, не плавающий. | Исследовать |
| Радар (HUD) рисуется поверх loading screen при выходе из миссии | При выходе из миссии (на loading screen перед возвратом во frontend) **радар продолжает рисоваться поверх экрана загрузки**. Замечено 2026-05-15. Раньше этого не наблюдалось — вероятно регрессия. Тот же класс что баг «briefing поверх loading screen» — HUD-элемент не скрывается на mission-end transition. Скорее всего radar draw call где-то не gated за состоянием выхода из миссии. Проверить как HUD скрывается на mission-complete vs mission-exit. | Исследовать |
| HUD-таймер не пропадает на mission-complete экране | По завершению миссии с машинкой (тайм-челлендж) на mission-complete экране (полноэкранная штука с текстом "миссия завершена") **продолжает рисоваться элемент HUD'а с таймером** (например `00:46`), хотя остальной HUD корректно скрыт. **В PieroZ-версии весь HUD пропадает** — значит регрессия в нашей кодовой базе. Замечено 2026-05-02. Скорее всего timer drawing call где-то не gated за `mission_complete`-флагом, в отличие от остальных HUD-элементов. **Возможно уже было пофикшено** в одной из последних итераций — **первым делом перепроверить, действительно ли воспроизводится сейчас**; если нет — переносить в resolved. | Перепроверить |
| Короткоживущие визуальные эффекты на высоком FPS: муззл-флэш тусклый, мерцание звёзд почти не видно | **Общий класс багов** — эффекты живущие 1 рендер-кадр становятся тусклыми/незаметными когда FPS растёт. На 25 FPS (оригинальный lock) 1 кадр = ~40 мс — нормальное визуальное «вспышка», глаз воспринимает как яркую. На 100+ FPS 1 кадр = ~10 мс — либо «мигнул и исчез», либо в часть кадров монитора попадает другое состояние → усреднение → визуально нет эффекта. **Регрессия из коммита fps-unlock**. **Известные проявления:** **(1, приоритет для 1.0) Муззл-флэш** — пламя из ствола рисуется на 1 кадр. Воспроизведение: любая миссия с пистолетом/AK — вспышка еле видна; залочить на 25 FPS — возвращается. Сейчас триггерится через `DT_FLAG_GUNFLASH` на DrawTween, ставится и съедается на следующем кадре в `figure_draw_held_item`. **(2, less critical) Мерцание звёзд** — звёзды на ночном небе должны заметно мигать (проверено на релизной PC-версии). У нас мерцания почти нет. **Фикс (общий, направление):** anchor любого короткоживущего визуального триггера к **игровому времени** (милисекунды), а не к рендер-кадрам. Для муззл-флэша: ввести таймер на персонаже (или global per-character muzzle-flash counter), декрементируется по game time, держит флаг активным ~40-50 мс. Для звёзд: пересмотреть `sky.cpp` — переключение состояния звезды по game time с фиксированной частотой (например 12-25 Hz). **Приоритет: муззл-флэш обязательно (виден в любой стрельбе); звёзды если успеем в том же фиксе. Если не успеем — звёзды в post-1.0.** | Исследовать |
| CRT-шейдер (эмуляция монитора) не зависит от физического размера экрана | Есть шейдерный эффект CRT (эмуляция ЭЛТ-монитора) — сам по себе хороший. Но сейчас он завязан на **виртуальный** размер экрана, а должен — на **физический** (реальное разрешение / плотность пикселей дисплея). На дисплеях с разной плотностью пикселей (Steam Deck vs внешний 4K vs обычный 1080p) эффект выглядит по-разному масштабированным. **Сделать:** пробросить физический размер/DPI в CRT-шейдер и нормировать параметры эффекта по нему. **Fallback если не получится быстро:** отключить CRT-шейдер в релизе (он опциональный визуальный эффект, отсутствие не блокер). До фикса/решения шейдер не должен попасть в релиз в нынешнем виде. | Исследовать (или отключить) |

---

## Управление

| Проблема | Описание | Статус |
|----------|----------|--------|
| Геймпад: R3 не работает на свежеподключённом DualSense пока не нажмёшь Enter с клавиатуры | **Репро (2026-05-20, RTA):** загрузил миссию RTA → нажал Enter (смена оружия с клавиатуры) → подключил DualSense по Bluetooth → ввёл чит на оружие и взял его → нажал **R3 на геймпаде для смены оружия — не сработало**. Затем нажал Enter с клавиатуры — оружие сменилось. После этого R3 на геймпаде **начал работать**. То есть R3 был "глухим" до тех пор, пока **один раз** не сработала смена оружия с клавиатуры. **Класс:** связан с двумя другими записями про **рассинхрон input state при подключении/отключении геймпада в середине геймплея**. Здесь конкретно: при mid-game BT-connect геймпада какой-то контекст input'а (snapshot инвентаря для `CONTROLS_rot_inventory`, либо флаг "active device", либо edge-detect состояние R3) не инициализируется. Первый `CONTROLS_new_inventory`-вызов с клавиатуры пересчитывает state и "будит" gamepad-путь. **Гипотеза по чинить:** на event подключения нового HID-устройства принудительно сбрасывать input snapshots (inventory rotation, last-edge per-button state) — аналогично disconnect-сценарию. Типичный сценарий "сел играть с клавиатуры, потом подключил геймпад", в котором первое нажатие R3 не реагирует. | Исследовать |
| Геймпад: R3 не прокручивает инвентарь при подборе оружия с открытым popup | **Репро (2026-04-15):** игрок нажимает R3 → открывается popup выбора оружия. Пока popup открыт, игрок подбирает новое оружие с земли. Новое оружие появляется в popup, но **выбрать его R3-вращением нельзя** — ротация крутит только по старому списку (до подбора). Закрыть popup и открыть заново — новое оружие выбирается нормально. **Workaround:** расфокус окна + фокус обратно снимает залипание (аналогично багу с курком DualSense). **Причина (установлена):** снапшот списка оружий в `CONTROLS_rot_inventory` не пересчитывается когда инвентарь меняется во время открытого popup, и rising-edge R3 крутит по устаревшему списку. **Фикс направление:** пересчитывать snapshot при изменении инвентаря или закрывать popup на подборе оружия. Сценарий нечастый, но при попадании выглядит как поломанная фича. | Исследовать (есть репро) |
| Музыка глючит при alt-tab окна | Когда окно уходит в фон (focus lost) и возвращается обратно (focus gained), музыка часто включается неправильно — играет не та композиция, либо несколько одновременно, либо переключается на чужой трек. Проявляется в разных моментах. | Исследовать |

---

## Геймплей

| Проблема | Описание | Статус |
|----------|----------|--------|
| Time overlay торчит на экране во время "The End" / outro после финала игры | **Репро (Finale1.ucm, 2026-05-08):** прохождение Day of Reckoning → играет outro-катсцена → появляется экран "The End" → запускается outro (credits / OS_hack), и **поверх outro продолжает рисоваться HUD-таймер с миссионным временем** (тот что обычно в правом верхнем углу). Точно такой же класс бага как уже исправленный «таймер lingers после завершения миссии» — там фикс сделан в `EWAY_process` clear на `GS_LEVEL_LOST | GS_LEVEL_WON` ([eway.cpp:4316](../new_game/src/missions/eway.cpp#L4316), `EWAY_hud_countdown_value = -1`). Но `EWAY_draw_hud_timers` ещё проверяет `EWAY_count_up_visible \|\| EWAY_count_up_add_penalties` — `count_up_visible` clear'ится в начале каждого tick'а `EWAY_process`, а после level-won весь цикл скипается → флаг остаётся в последнем значении, но `count_up_add_penalties` не сбрасывается. **Гипотезы для фикса:** (1) защитный clear `EWAY_count_up_add_penalties = UC_FALSE` + `EWAY_count_up_visible = UC_FALSE` на entering outro / `the_end == UC_TRUE`; (2) гард в самой `EWAY_draw_hud_timers` — не рисовать таймеры если `the_end \|\| GAME_STATE & GS_LEVEL_WON` уже произошёл; (3) убрать вызов `EWAY_draw_hud_timers` из render frame во время outro. Похожая ситуация — выполненный фикс countdown'а — взять за образец. Финальный экран игры, выглядит неаккуратно. | Исследовать |

---

## PieroZ

| Баг | Коммит | Описание |
|-----|--------|----------|
| Bat/Balrog: выход за массив `BAT_state_name` | PieroZ `021c515` | Массив `BAT_state_name` на 1 элемент короче — **потенциальный краш на босс-уровнях** (Insane Assault, где активен BAT-класс / Балрог). Фикс тривиальный — расширить массив на 1, портировать из PieroZ-коммита. **Обязательно после фикса протестить Insane Assault полностью** — убедиться что Балрог и его состояния продолжают работать корректно, что фикс не сломал поведение босса. | Реализовать (с регресс-тестом) |

---

## Настройки и конфигурация

| Проблема | Описание | Статус |
|----------|----------|--------|
| Выпилить настройку языка (оставить английский) | Сейчас language захардкожен в `config.h` как `OC_LANGUAGE_FILE`, попытки смены ломали отрисовку (странный шрифт у значений опций при не-английском, ASan-краш в `FRONTEND_MissionHierarchy`). Архитектурно: оригинальные `lang_*.txt` содержат строки на фиксированных позициях, наши новые пункты меню туда не вписываются. **Решение для 1.0:** убрать настройку языка из меню и из конфига вообще, всё что не покрывается ресурсными файлами — хардкод на английском (наши доработки). **Смена языка для пользователя** — только через патч языковых файлов (положить нужный `lang_*.txt` рядом с игрой). **⚠️ Перед выпиливанием проверить релизную PC-версию:** есть ли там встроенная смена языка через меню. Если есть и реализовать у нас быстро (например подключить наши хардкод-английские строки как fallback к не-английскому ресурсному файлу) — сделать. Если что-то ломается или долго — выпиливать настройку, оставить только английский + патч-файлы. Полная локализация — post-1.0 (см. соответствующую запись). | Реализовать (выпил + sverit с релизом) |
| Система конфига — минимально-необходимая доводка | Базовая реализация `OpenChaos.config.json` (рядом с exe) готова (2026-04-27). **Принцип для 1.0 — минимально необходимое:** (1) что **быстро сделать + важно** — реализуем (gamma после фикса в post — нет, см. отдельную запись; language — см. отдельную запись; missing-resources — см. отдельную запись); (2) что **быстро не получится и не критично** — убираем из UI и из конфига вообще, чтобы не было "настройка есть, но не работает / расходится с реальностью". **⚠️ Порядок работ:** трогать конфиг **только после** доработки системы управления (L2 + стрелки для перекатов и пр. — см. запись «Переработка управления»). До этого настройки управления нет смысла шлифовать. **Конкретные доработки конфига для 1.0:** (a) добавить настройку **FPS limit** (значения 60/120/144/240/Unlimited; см. идею в stage12.md — вопрос UI vs конфиг-only решается тут); (b) gamma — убрать из меню (см. отдельную запись); (c) language — убрать (см. отдельную запись); (d) почистить ENV → config где ещё используется; (e) detail toggles — если важны для производительности (Steam Deck), вернуть в UI; иначе оставить только в JSON. Подробности pending-доработок → [`../new_game_devlog/config_json/README.md`](../new_game_devlog/config_json/README.md). | В работе |
| Gamma коррекция — убрать из настроек | `ge_is_gamma_available` возвращает false, меню яркости не работает. **Решение для 1.0:** убрать настройку из меню (и из конфига) — не показывать пользователю фичу которая не работает. Полноценный фикс gamma вынесен в post-1.0 (см. соответствующую запись там). | Убрать из UI/конфига |
| Пройтись по всему коду и убедиться что ничего отладочного не торчит в релизе | Все dev-only хоткеи, оверлеи, панели и прочие debug-возможности должны быть **спрятаны под режим `bangunsnotgames`** (`allow_debug_keys` gate). **Первая итерация 2026-04-17 сделана:** F2 gamepad overlay удалён; F11 cloud toggle переехал на F4 (убран дублёж); все известные debug-клавиши под гейтом (F11 panel, Shift+F12 FPS, F12 weapon-spawn, V version, Ctrl+L, Ctrl+Q, step-once, Shift+M mine-at-mouse и т.д.); F1 показывает легенду всех клавиш режима; F9 (консоль) — единственный не-гейтед вход для активации bangunsnotgames. **Осталось:** ещё раз прошерстить **весь** new_game/src (не только клавиши — также: отладочные CONSOLE_text/MSG_add, читы через консоль типа `win`/`lose`/`cam`/`tels`/`crinkles`, `ControlFlag && ShiftFlag` debug-ветки в process_controls, оверлеи типа `stealth_debug` которые могут оставаться активными после выхода из debug-режима) на предмет забытой dev-функциональности. **⚠️ Делать в самом конце 1.0**, после всех остальных изменений по кодовой базе — иначе в процессе правок можем добавить новое, что снова придётся прочёсывать. **Также:** некоторые вещи которые **никогда не должны работать в релизе** (даже под debug-режимом) — поубирать **под препроцессорный флаг** (debug-only build). **Пример: split screen** — он всё равно крашится (см. в post-1.0 запись «Краш в debug-режиме на `[` / `]` (camera-2 cycle)»), нет смысла его оставлять доступным в release-билде; убирать под `#if defined(DEBUG)` или аналог. Любая отладочная возможность в release-билде = не ок. | Сделать в конце 1.0 |

---

