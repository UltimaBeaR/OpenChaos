# Этап 10 — Доработки функционала

**Цель:** функциональные и визуальные улучшения поверх стабильной базы.

- ~~Поддержка xbox контроллера~~ → вынесено в [этап 5.1](stage5_1.md) (Xbox + DualSense)
- Доработки управления (менее топорное, привычнее для современных игроков)
- Увеличение размера карты: убрать хардкод на маленький размер, возможно стриминг карты
- Геймплейные улучшения: улучшенное управление авто, улучшенный захват цели, etc
- Визуальные улучшения (без замены ассетов): динамическое освещение и тени,
  постпроцессинг (bloom, color grading, SSAO), сглаживание (AA), улучшенная фильтрация
  текстур, улучшенные частицы и эффекты, улучшенный туман/атмосфера
- Добавление дополнительного игрового контента (потребуется рабочий игровой редактор)
- Встроить некоторый функционал из существующих модов сообщества (Maddi Edition и др.)
  как опциональные настройки: first person camera, отключение auto-aim/радара и т.д.
  Моды сделаны через бинарные хаки оригинального exe, несовместимы с ремейком напрямую,
  но их фичлист — готовый источник идей что сообщество хочет видеть.
  Ссылки на моды: `new_game_devlog/community_links.md`
- Добавить поддержку сторонних модов (совместимость с оригиналом сделать не получится)
- Любые другие улучшения и доработки, в том числе предложенные сообществом

## Улучшения управления

- **Полный перенос функционала крестовины (D-pad) на левый стик** — сейчас ряд действий работает только через крестовину и не срабатывает со стика. Конкретные проблемы:
  - **Перекаты в бою:** уход из боя перекатом в сторону (стрелка в бок + прыжок) не работает со стиком — приходится жать крестовину, что неудобно в разгар драки.
  - **Ходьба назад:** вне боя иногда нужно идти назад (например спуск по лестнице задом) — сейчас только через крестовину.
  - Цель: полностью убрать необходимость использования крестовины для движения. Крестовину по стандарту современных игр перепрофилировать на смену оружия и т.п.
  - Требует пересмотра всей схемы маппинга стика — как стик взаимодействует с action tree (бой, паркур, ходьба).
- **Кувырок вместо прыжка в сторону** — при беге + резкий поворот стиком + прыжок персонаж делает кувырок (flip) вместо прыжка в сторону. Особенно мешает при активном паркуре — резкие нажатия стика вбок неизбежны и кувырки всё портят. Нужно пересмотреть приоритеты/пороги в action tree чтобы прыжок в сторону срабатывал предсказуемо, а кувырок — только при явном намерении.
- **Раздельные кнопки для действий (после 1.0)** — сейчас многие действия на одной кнопке (action). Разделить: действие (interact) на одну кнопку, бег на другую, ползание на третью и т.д. Для клавиатуры И геймпада. Полная переработка input mapping.
- **Управление мышью (после 1.0)** — опциональный режим при игре с клавиатуры: поворот персонажа мышью + прицеливание/стрельба мышью. Стандартный WASD+мышь как в современных 3rd person action.

## UI настроек для экспериментальных фич

Сейчас feature flags управляются хардкодом в `feature_flags.cpp`. В будущем — UI в настройках игры (отдельная вкладка "Experimental" / "Extras") для включения cut/ext фич без перекомпиляции. Сохранение в конфиг-файл (`openchaos.cfg`). Описание всех флагов → [`FEATURE_FLAGS.md`](../FEATURE_FLAGS.md).

## Подсказки управления и внутриигровая справка

Игра неочевидна в управлении — многие действия (перекаты, удары назад, комбо, паркур-приёмы) сложно обнаружить самостоятельно.

- **Контекстные подсказки кнопок** — расширить идею PS1-подсказок (см. [known_issues](known_issues_and_bugs.md), строка 168) на всю игру: показывать подсказки везде где уместно — в меню, в бою, при взаимодействии с объектами, при обучении. Адаптировать под текущее устройство ввода (геймпад / клавиатура).
- **Визуальная схема раскладки в опциях** — пункт в настройках показывающий полную раскладку управления: какая кнопка что делает, с визуальным изображением контроллера/клавиатуры.
- **Внутриигровая вики/справочник** — доступная из паузы или меню база знаний по возможностям игры: боевые приёмы (на бегу + влево + прыжок = перекат, в бою удар + назад = удар ногой назад, и т.д.), паркур-приёмы, взаимодействия. Формат: краткие карточки с описанием и раскладкой кнопок.

## Геймплейный баланс

- **Неестественные тайминги комбо-ударов** — особенно руками: окна для ввода следующего удара в комбо неинтуитивны, сложно попасть в нужный момент. Возможно тайминги завязаны на прокачку статов (по картам раскиданы бонусы) — нужно проверить. Задача: сделать тайминги комбо более отзывчивыми и естественными, но не сломать саму идею skill-based комбо (чтобы не превратилось в button mashing). Возможное решение: HUD-индикатор тайминга комбо (полоска/маркер показывающий окно ввода следующего удара) — чтобы игрок визуально понимал когда нажимать. Требует изучения action tree и связи с системой прокачки.
- **Кулдаун боевого перемещения обходится** — в бою есть кулдаун на перемещение в одну сторону (например влево), но его можно обойти: (1) чередование направлений (влево → вправо) — второе нажатие без задержки; (2) спам "влево + удар + влево + удар" — перемещение влево идёт очень быстро. Нужен общий кулдаун на любое боевое перемещение, не только на повтор в ту же сторону.
- **Увеличить радиус удара ногой по лежачему** — сейчас нужно подойти слишком точно к лежащему противнику чтобы удар ногой сработал. Увеличить дистанцию срабатывания для удобства.
- **Перекидывание + арест слишком эффективны** — комбо "вперёд + удар" (перекидывание) + "действие" (арест) позволяет быстро класть толпы врагов без реальной драки. Убивает необходимость использовать боевую систему. Варианты нерфа: (1) задержка/кулдаун на перекидывание (заодно решит проблему случайных перекидываний когда хочешь просто ударить вперёд); (2) ограниченное количество арестовываний за миссию; (3) комбинация обоих подходов. Продумать детали позже.

## Визуальные изменения (план)

- **Поддержка высоких разрешений + выбор в настройках** — сейчас фиксированное разрешение 640×480. Нужно: поддержка произвольных разрешений (1080p, 1440p, 4K), выбор разрешения в меню настроек, сохранение в конфиг. Связано с `ge_update_display_rect` (resize окна, этап 7 TODO).
- **Сглаживание (anti-aliasing)** — сейчас AA отсутствует, видны лесенки на краях геометрии. Варианты: MSAA (самый простой, через OpenGL multisample), FXAA (постпроцесс), или TAA. MSAA — минимальная реализация для начала.
- **Убрать пунктирные линии прицела врага** — серые пунктиры показывающие что враг целится огнестрельным оружием. Есть в PC релизе, отсутствуют в PS1 версии. Выглядят топорно, планируется удалить.

## Оптимизации рендерера

- **Draw call batching** — сейчас каждый PolyPage slot делает отдельный draw call со своим VBO/EBO/VAO (per-slot, как D3D6 оригинал). Батчинг нескольких draw calls в один большой VBO с одним `glDrawElements` уменьшит количество draw calls с сотен до единиц за кадр. Требует: общий VBO с sub-allocation, offset-based indexing.
- **GPU-transform для 3D геометрии** — перевести всю трёхмерную отрисовку (мир, объекты, персонажи, эффекты) на аппаратные трансформации вершин через шейдеры вместо CPU-transform. Сейчас `ge_draw_indexed_primitive_lit` и `ge_draw_multi_matrix` делают CPU-transform (World×Projection), а результат рисуется как TL (pre-transformed) вершины. Это исторический подход из D3D6 эпохи. Переход на GPU-transform даст прирост производительности и упростит pipeline. **Применять только к реальному 3D** — UI/HUD оставить на CPU-transform (TL path). Техдолг из этапа 7: `lit_vert.glsl` не работает из-за D3D/GL clip space конвенций. Сложная доработка — потребуется переработка матриц, viewport transform, depth mapping. Опционально, после релиза 1.0.

## Кастомные карты/миссии (AI-assisted level design)

Идея: Claude Code в роли разработчика кастомных уровней/миссий, с пользователем в роли креативного директора.

### Подготовка

- **Восстановить редактор игры.** Код редактора вырезан из new_game, придётся тащить из оригинала (`original_game/`). Собрать отдельно, сделать рабочую версию. Не обязательно 1:1 оригинальный редактор — возможно другой, более удобный.
- **Референс:** у PieroZ есть рабочая версия редактора (записывал видео) — глянуть для понимания workflow.

### MCP-сервер для редактора

- Весь функционал редактора обернуть в MCP-сервер целиком. Claude Code через MCP взаимодействует с редактором: размещает объекты, задаёт параметры, управляет геометрией карты и т.д.
- Специальный режим работы Claude Code: подключение к MCP редактора, скиллы и документация для level design.

### База знаний для level design

Перед созданием миссий — сформировать полную базу знаний:
- Анализ всех существующих уровней: структура, объекты, эффекты, скрипты
- Каталог ассетов: как выглядят здания, объекты, персонажи, транспорт (визуальное понимание)
- Типы объектов, доступные эффекты, возможности размещения
- Паттерны геймдизайна: как устроены миссии, какие механики комбинируются, как строится сложность
- Документация и скиллы для Claude Code по всему вышеперечисленному

### Workflow создания миссии (итеративный процесс)

Claude делает ~95% работы, пользователь направляет:
1. **Сюжет и концепция** — Claude предлагает варианты сюжета/сценария миссии, пользователь выбирает направление
2. **Планировка уровня** — Claude проектирует layout, предлагает варианты ("поставить здание сюда — будет красиво?"), пользователь одобряет/корректирует
3. **Детализация** — Claude прорабатывает детали: расстановка врагов, скрипты событий, эффекты, освещение
4. **Показ результата** — Claude показывает промежуточный результат пользователю (скриншоты, описание)
5. **Итерация** — пользователь даёт фидбек общими мазками, Claude дорабатывает

Принцип: все сомнительные решения обсуждаются с пользователем. Высокоуровневое направление выбирает пользователь, детали и реализация — Claude.

## Быстрые сохранения внутри миссии

Миссии длинные и сложные — при гибели игрок вынужден проходить всю миссию заново, что фрустрирует. Межмиссионные сохранения (существующая система) не трогаем.

**Идея:** ограниченные быстрые сохранения (чекпоинты) внутри одной миссии. Доступны только в рамках текущего прохождения миссии — при выходе из миссии или начале новой сбрасываются. Возможны ограничения (количество сохранений на миссию, кулдаун, и т.д.) чтобы не превратить игру в save-scum. Детали дизайна — продумать позже.

## Референсы

- `PieroZ/MuckyFoot-UrbanChaos` коммит `0f2e69d` — WIP по bat/bane боссу + взрывы (bangs). Глянуть при доработке боссов/эффектов.

