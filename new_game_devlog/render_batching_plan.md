# План: батчинг draw-вызовов и перенос трансформа на GPU (персонажи/«things»)

> **⚠️ ВАЖНО ДЛЯ СЛЕДУЮЩЕГО АГЕНТА:** План ниже — **ориентир, не директива**.
> Это рабочая гипотеза по результатам экспериментов 2026-05-21. **Не
> выполнять буквально**. Эксперементируй сам: измерь, проверь, скорректируй
> на основе реальных данных в перф-панели. Если данные не подтверждают
> ожидания плана — **меняй направление**. Пользователь надолго отойдёт и
> вернётся к итоговому результату, не к ходу работы. Жёстко не следовать
> плану — следовать здравому смыслу с использованием плана как контекста.
>
> **СТАТУС (обновлено 2026-05-21, autonomous run):**
> - **Этап 1** (дешёвый батчинг персонажей) — **ЗАВЕРШЁН**.
> - **Этап 2** (полный GPU-скиннинг, персистентный VBO, тени и отражения
>   на GPU, авто-риг) — **ЗАВЕРШЁН**.
> - **Этап 3** (оптимизация финального альфа-flush'а):
>   - **3.1** batch-safe pre-pass — **СДЕЛАНО**.
>   - **3.3** near/far split альфа-сорта — **СДЕЛАНО**.
>   - **3.4** opaque-pass diagnostics — **СДЕЛАНО**: в `poly.cpp`
>     добавлены `s_opaque_page_polys` / `s_opaque_page_batches` +
>     top-5 overlay с маркерами `[2P-base]` / `[2P]`. Городская сцена:
>     ~102 уникальных opaque-страниц, в среднем 55 poly/draw — батчи
>     жирные, фрагментации нет. Топ: LEAF 733, page#220 [2P-base] 649,
>     page#33 616, page#32 478, page#280 200. Дальнейшая оптимизация
>     opaque потребует texture-array / atlas (большой рефакторинг
>     ассет-pipeline'а).
>   - **3.2 SELF_ILLUM + WINDOW в шейдер** — **СДЕЛАНО** (визуально
>     проверено, перф подтверждён). Реализовано через **per-page overlay
>     binding + композиция в общем фрагмент-шейдере**, БЕЗ расширения
>     vertex формата:
>     - `common_frag.glsl` принимает `u_overlay_mode` (0/1/2) + сэмплер
>       `u_overlay_tex` (texture unit 1); композирует overlay поверх
>       diffuse в одном проходе. Для SELF_ILLUM — `step(7/255, alpha)`
>       mask + mix (эквивалент легаси Decal+alpha-test). Для WINDOW —
>       стандартный alpha-blend mix.
>     - `ge_set_overlay_state(page, mode)` — public API; принимает
>       PAGE INDEX (не GL handle), резолвится через `s_textures[page].gl_id`
>       на момент draw'а.
>     - `PolyPage::m_OverlayPage` / `m_OverlayMode` — per-page метаданные,
>       выставляются в `POLY_init_render_states` для всех 2PASS базовых
>       страниц. `PolyPage::Render` и `DrawBatchedPolys` биндят их перед
>       draw call'ом.
>     - `ge_draw_indexed_primitive` (TL direct path: fonts/sky/outro)
>       ресетит overlay state в 0 — защита от утечки между PolyPage и
>       внешними draw'ами.
>     - `POLY_add_quad_fast` / `triangle_fast` / `nearclipped_triangle`
>       — `goto second_page` удалён вместе с диагностическими счётчиками
>       `flush.2pass_*` (Phase D cleanup завершён).
>     - **Подсистемы перенесены:** SELF_ILLUM (буква `I` — подсвеченные
>       окна, неон, фары — ~736-758 polys/frame). WINDOW (буква `D` —
>       стёкла зданий) — **в игровых данных не используется** (проверены
>       все textype.txt по world1-world20, ни одной 'D' метки); код
>       поддерживает на случай custom-контента.
>     - **НЕ перенесено:** ENVMAP — требует per-vertex env_uv от нормали,
>       что требует расширения vertex формата. Отдельный шаг, отложен.
>
> **Подтверждённый перф (городская сцена, до/после):**
>
> | Метрика | До | После | Δ |
> |---|---|---|---|
> | `fx_flush` total CPU | 0.69 ms | 0.62 ms | **−10%** |
> | `fx_flush` ge-CPU | 0.19 ms | 0.15 ms | **−21%** |
> | `fx_flush` GPU | 0.71 ms | 0.50 ms | **−30%** |
> | `flush.draws` | 179 | 169 | −10 |
> | `flush.polys` | 7300 | 6536 | **−764** (матчит ~736 SELF_ILLUM убранных из 2-го прохода) |
> | `flush.alpha_batches` | 62 | 58 | −4 |
> | `flush.alpha_polys` | 196 | 191 | −5 |
>
> **Зафикшено в процессе:** была критичная ошибка с типом `m_OverlayTexture`
> — `TEXTURE_get_handle()` возвращает уже GL gl_id, а не page index, но
> я индексировал `s_textures[handle]` (массив по page index). Это давало
> чёрные квадраты на месте окон. Фикс: PolyPage хранит **page index**
> (`m_OverlayPage` типа `int32_t`), backend резолвит на момент draw'а.
>
> **Следующие возможные шаги (по приоритету):**
> 1. **Phase B — ENVMAP в шейдер** (~381 polys/frame): требует per-vertex
>    env_uv → расширение vertex формата. Меньший win чем SELF_ILLUM
>    (1 batch уже после 3.1 batch-safe, остаётся только cost второго
>    прохода растеризации). Перенести когда будет время на инвазивный
>    рефакторинг vertex format.
> 2. **Phase D — СДЕЛАНО.** Удалены `flush.2pass_*` счётчики, dead
>    `[2P]` overlay-marker в alpha overlay (overlay-страницы больше не
>    submitting), обновлены комментарии. Флаги `POLY_PAGE_FLAG_2PASS`
>    оставлены — используются как идентификатор страницы-базы в
>    `is_2pass_base` и `[2P-base]` маркере. WINDOW_2ND флаг инертен
>    (page+1 нет polys), не трогаем.
> 3. **Opaque path optimization** (из 3.4 разведки): 102 уникальных
>    opaque-страниц = 102 state changes/draws. Возможный путь —
>    texture-array / atlas для группировки building-текстур (page#33,
>    #32, #220, #280 etc.). Большой рефакторинг ассет-pipeline'а.

Рабочий план оптимизации рендера. Без правок кода — только анализ, риски,
этапность. Связанные записи (не дублировать, только ссылки):

- [stage13.md](../new_game_planning/stage13.md#L96) §«Оптимизации рендерера»
  (стр. 96-99) — короткие пункты «Draw call batching» и «GPU-transform».
- [stage13_future_ideas.md](../new_game_planning/stage13_future_ideas.md)
  §«Скелетные анимации персонажей» и §«Перевод рендера на Vulkan»
  (нюансы bindless/текстурных массивов; god-shader — не главный рычаг).
- [known_issues_and_bugs.md](../new_game_planning/known_issues_and_bugs.md) —
  пометка про CPU-transform техдолг.

---

## 1. Контекст: что показал перф

Лог снят в городе (Urban Shakedown, вход в полицейский участок, миссия RTA),
улица, персонажи в кадре. FPS 52 (~19 мс/кадр).

- Сплит кадра: CPU ge 13% · CPU non-ge 17.6% · **GPU 69%**.
- Доминирующая стадия: **`render.scene.things` — 5.71 мс GPU (61% всего
  GPU)**, ge-CPU 0.58 мс, non-ge 0.96 мс.
- `render.scene.fx` — ge-CPU **1.09 мс** (62% всего ge-CPU кадра!) + 1.82 мс GPU.
- Счётчик **`render.drawcalls = 5838`** за кадр.

`GPU wait (glFinish) 9.38 мс` в логе — цена самого замера (per-scope glFinish),
не игра; в релизе без perf её нет.

### Замер Этапа 0 (атрибуция 7507 вызовов, сцена RTA)

| Источник | Вызовов | Доля |
|---|---|---|
| `figure` (персонажи, по треугольнику) | **4187** | 56% |
| `world` (поли-страницы) | 397 | 5% |
| `farfacet` | 10 | ~0% |
| `sky` | 0 (день) | 0% |
| неучтённое (fx + тени + UI/шрифты + сама панель) | **≈ 2900** | 39% |
| `mm.calls` (вызовов `ge_draw_multi_matrix`) | 130 | — |

Вывод: путь персонажей раздувает **130 MM-вызовов → 4187 draw-вызовов
(×32, вызов-на-треугольник)** — главный единичный источник. Мир уже
забатчен (397). ~2900 неучтённых — отдельная крупная цель (эффекты/тени/UI,
Этап 3), подтверждает необходимость обобщённого батчера.

## 2. Факты по коду (проверено, не гипотеза)

**Мир / статика уровня — уже забатчено.** `PolyPage::Render()`
([polypage.cpp:325](../new_game/src/engine/graphics/pipeline/polypage.cpp#L325))
готовит вершинный буфер (`ge_vb_prepare`) и рисует всю поли-страницу **одним**
`glDrawElements`. Сотни полигонов = один вызов. Трогать этот путь смысла мало.

**Персонажи / фигуры — вызов на КАЖДЫЙ треугольник, без персистентного
буфера.** `ge_draw_multi_matrix`
([polypage.cpp:416](../new_game/src/engine/graphics/pipeline/polypage.cpp#L416))
идёт по стрипу и на каждый треугольник зовёт `ge_draw_indexed_primitive(3
верш, 3 индекса)`
([polypage.cpp:543](../new_game/src/engine/graphics/pipeline/polypage.cpp#L543)),
а тот на каждый вызов делает `glBufferData(... STREAM)` + `glDrawElements`
([core.cpp:1189](../new_game/src/engine/graphics/graphics_engine/backend_opengl/game/core.cpp#L1189)).
Буфер перезаливается на каждый треугольник. Это путь персонажей (`figure.cpp`)
→ стадия `render.scene.things`. Почти весь объём 5838 вызовов — отсюда.

**Инстансинг — не используется** нигде (только объявления в GL-загрузчике).

**Шейдер уже один.** Вся 3D-графика (мир, персонажи, эффекты) идёт через
единственную программу `s_program_tl`. «Материалы» — это юниформы (текстура,
туман, альфа, блендинг) через `set_frag_uniforms`, не отдельные шейдеры.
Программа кешируется, переключается редко. Полноэкранные пост-эффекты
(wibble/crt/composite/video) — отдельные программы, но не per-object.

**Вывод про «мультиматериал».** Делать god-shader незачем — он де-факто уже
есть. Со сменой стейта это не поможет: проблемы «дёргаем glUseProgram» нет.
Реальная per-draw цена: перезалив буфера каждый вызов + смена текстуры +
перезалив юниформов. Дробление вызовов — по **текстурам**, не по шейдерам.

**Ключевое следствие.** В буфер персонажа сейчас кладутся вершины, **уже
преобразованные в экран**, с посчитанным на CPU освещением (half-Lambert по
костям) и туманом. Это per-frame данные по своей природе. «Закешировать
буфер как есть» нечего — стабильного содержимого там нет. Персистентный буфер
для персонажей **неразрывно связан** с переносом трансформа/освещения/тумана
на GPU. Это одно дело, не два.

## 3. Цель

Обвалить число draw-вызовов на пути персонажей/«things» (главный CPU/драйвер
кост на слабом железе — Steam Deck, интегрированная графика) и снять
CPU-трансформ. Слой батчинга проектировать **обобщённым и переиспользуемым**:
эффекты (и потенциально другие пути) позже подключатся к нему же — заложить
это в API сразу, не делать character-only.

## 4. Этапность

### Этап 1 — дешёвый батчинг без шейдера (делаем сейчас, низкий риск)

Не трогая CPU-трансформ / освещение / туман. Вместо вызова-на-треугольник —
накапливать **весь персонаж** (а лучше всех персонажей с одной текстурой) в
один буфер и рисовать **одним вызовом на персонажа/текстуру**. Содержимое
буфера то же (TL-вершины, посчитанные на CPU как сейчас) — меняется только
**как сабмитим**: копим → один `glDrawElements` вместо N.

- Выигрыш: число draw-вызовов падает в разы. Бьёт в CPU/драйвер — профиль
  боли Steam Deck.
- Риск визуала: минимальный (данные вершин не меняются, меняется только
  группировка сабмита). Сохранить порядок прозрачных (back-to-front) —
  непрозрачные группировать свободно (z-буфер), прозрачные нельзя
  переупорядочивать внутри батча.
- Слой делать **обобщённым**: «накопитель TL-вершин по текстуре/стейту →
  флаш одним вызовом». Не привязывать к персонажам — чтобы эффекты потом
  легли на тот же накопитель.

### Этап 2 — глубокий шаг: скелетная анимация + GPU-скиннинг (экспериментально)

> Детальный план Этапа 2 вынесен отдельно:
> [skeletal_skinning_plan.md](skeletal_skinning_plan.md). Ниже — краткое
> резюме; актуальная этапность/риски — в том документе.

Сейчас скелетной анимации нет; её добавление и так в планах
([stage13_future_ideas.md](../new_game_planning/stage13_future_ideas.md)
§«Скелетные анимации персонажей»). Решение: **двинуться экспериментально в
эту сторону** — перевести движения персонажей на скелетный режим, скиннинг
делать на GPU.

- Модельный меш уникального персонажа заливается в **персистентный VBO один
  раз** (при загрузке уровня / первом использовании), дальше не
  пересоздаётся. Экземпляры рисуются переиспользуя общий буфер.
- Трансформ + скиннинг + per-vertex освещение + туман — в вертексном шейдере
  (матрицы костей как юниформы / UBO).
- Скиннинг решает **не только перф, но и визуал**: исчезают щели на стыках
  «болванок» (документированный артефакт — голова/шея и т.п.).
- **Фолбэк, если скиннинг не выйдет:** грузить модель один раз, а вертексным
  шейдером двигать **куски тела как жёсткие части** (rigid per-part
  трансформ — по матрице на болванку, без весов). Дешевле и проще полного
  скина, всё ещё персистентный буфер + GPU-трансформ. Но т.к. переделка всё
  равно крупная — сначала пробуем полноценный скиннинг (он закрывает и перф,
  и визуал), фолбэк — запасной вариант.

### Этап 3 — оптимизация альфа-flush'а (текущая)

Большой POLY_frame_draw в конце кадра жрёт ~58-88% всего ge_* CPU в кадре
(зависит от сцены) — диагностика на 2026-05-21 показала среднюю фрагментацию
альфа-сорта ~2-4 полигона на draw-вызов. Причина: при строгом back-to-front
полигоны разных страниц перемежаются в Z, каждая граница страниц требует
смены state и нового draw call'а. Цель: уменьшить количество draw call'ов и
ge_* CPU не ломая визуал (особенно строгий порядок плотных альфа-сцен —
листва деревьев, кусты, лавки).

Стадия разбита на **3 шага в строгом порядке**. Без выполнения предыдущего
следующий теряет смысл или вреден.

---

#### Шаг 3.1 — batch-safe pre-pass для «жирных» страниц (СДЕЛАНО 2026-05-21)

Страницы с большим поликаунтом и **внутренне-безопасным порядком** (полигоны
не должны строго сортироваться между собой) выносятся в отдельный батч-проход
**между opaque-loop и альфа-collect**. Каждая такая страница даёт **один**
DrawBatchedPolys вместо десятков мелких в общем сорте.

В коде: блок `batch_safe_pages[]` в `POLY_frame_draw` (poly.cpp). Текущий
список:
- `POLY_PAGE_SHADOW` — проекции силуэтов теней персонажей
- `POLY_PAGE_SHADOW_SQUARE` — квадраты теней
- `POLY_PAGE_SHADOW_OVAL` — овальные блоб-тени дальних персов
- `POLY_PAGE_ENVMAP` — env-mapped зеркальные поверхности (**временно**;
  см. Шаг 3.2 — должен исчезнуть после переноса в шейдер)

**Порядок принципиален:** ПОСЛЕ opaque (земля/здания/кузова есть), ДО
альфа-sort (листва/дым ложатся сверху). Если поставить ДО opaque — opaque
перекрасит и шаги-safe-полигоны исчезнут (баг 2026-05-21, исправлен).

**Риск visual:** строгий per-poly z-order между batch-safe и сортируемыми
страницами потерян. Допустимо для теней (subtract на пол — приближённо
коммутативно) и для env-mapped стёкол машин в типичных сценах (стёкла
обычно фоном к зданиям/дороге, не к листве). Если кто-то добавляет НОВУЮ
страницу в этот список — обязан проверить визуально, не появляется ли
неправильное наложение со сортируемыми страницами.

**Эффект:** ~30-50% экономия draw call'ов в альфа-flush'е (~90-150 batches
→ ~30-60 в типичных городских сценах).

---

#### Шаг 3.2 — перенос multi-pass эффектов в shader (ОТЛОЖЕНО)

> **⚠️ ОТЛОЖЕНО (2026-05-21).** По диагностике в текущих сценах win
> оказался **скромным** (см. §«Шаг 3.2 — почему отложено» ниже). Сначала
> делается **Шаг 3.4** (opaque investigation). По его результатам
> решается стоит ли вообще браться за 3.2.
>
> **Если когда-либо будем делать 3.2:** реализация **через разделение
> шейдеров** (отдельный vertex format + отдельный shader path для
> multi-pass-капабельных страниц). НЕ расширять глобальный `GEVertexTL` —
> это затронет UI/text/font/sky/font_atlas/outro и т.д., слишком инвазивно
> и рискованно. Изоляция через `use_extended_format` flag на PolyPage.

##### Шаг 3.2 — почему отложено (диагностика 2026-05-21)

Гипотеза до диагностики: топ-5 страниц альфа-сорта (page#853, #857, #292,
#856, и т.п. — текстурные мирные страницы) — это **page+1** 2PASS-overlay'ев
(SELF_ILLUM). Если так, multi-pass shader-port убрал бы их из альфа-сорта
целиком → win ~3/4 текущей фрагментации.

Проверка через маркер `[2P]` рядом с именем страницы (в overlay):
страница помечается `[2P]` если предыдущая страница (idx-1) имеет
`POLY_PAGE_FLAG_2PASS`. В текущей сцене **ни одна** из топ-5 не имеет
этого маркера — значит они НЕ являются 2PASS-overlay'ями. Это
обычные alpha-декали мира (баннеры, окна без подсветки, прозрачные
текстуры на фасадах).

Параллельно `flush.2pass_polys = 758` ↔ `flush.alpha_polys = 196`. Из
758 2PASS-overlay-поликвадов в alpha-sort попадает ≤196. Остальные 500+
идут через **opaque-проход** (потому что у них `POLY_PAGE_FLAG_SELF_ILLUM`
+ additive blend → не требуют сортировки).

**Вывод:** multi-pass port уберёт ~500-700 дублирующих рассеризаций
с GPU и ~500-700 микро-сабмитов с CPU, но НЕ уменьшит number of draws
существенно (overlay-поликвады раскидываются по тем же страницам что
и базовые → opaque-pages count не меняется). Win:
- CPU: ~50-100µs.
- GPU: ~0.1-0.3ms.
- Draws: -5-15 (модестно).

Vs ~8-16 часов работы и риск регрессий → **ROI плохой для текущего
момента**. Сначала надо найти где `~112 opaque draws` дороги — это
основной кост сейчас.

**Контекст.** Архитектура рендера здесь — наследие D3D6 (1999), когда не
было ни шейдеров, ни multi-texturing. Многие визуальные эффекты сделаны
**отдельными проходами по уже отрисованной геометрии**: одна и та же грань
сначала рендерится со своей diffuse-текстурой, а потом ПОВТОРНО на отдельную
POLY-страницу с blend `Modulate`/`Additive` чтобы наложить эффект сверху.
Каждый такой второй проход — это дополнительные draw-вызовы и попадание
в альфа-сорт (если поверхность семантически прозрачная для модулятора).

**Сегодня (с шейдерами OpenGL 4.1) это анахронизм.** Один меш = один draw,
шейдер сэмплит diffuse + модулятор + что угодно и комбинирует в одном
fragment-проходе. Никакого второго прохода, никакой отдельной POLY-страницы,
никакого попадания в альфа-сорт.

**Стратегия:** один обобщённый «multi-texture opaque path» шейдер, который
умеет diffuse + (опционально) env-map + (опционально) self-illum overlay +
(опционально) window overlay в одном fragment-проходе. Один раз построить,
переключить на него все 4 multi-pass-источника пачкой.

##### Полный список multi-pass источников в коде (разведка 2026-05-21)

**(1) ENVMAP — `mesh.cpp:395-499`**
- Триггер: `p_obj->flag & PRIM_FLAG_ENVMAPPED` и `FaceFlags & FACE_FLAG_ENVMAP`.
- Что делает: для грани сначала сабмит на свою diffuse-страницу (первый
  цикл `mesh.cpp:265-393`), потом *перезаписывает* UV в `POLY_buffer[]`
  на sphere-mapped (`u = (nx*0.5)+0.5; v = (ny*0.5)+0.5`, нормаль через
  `cam_matrix * mesh_matrix`) и сабмитит ту же грань ПОВТОРНО на
  `POLY_PAGE_ENVMAP` с `Modulate` blend.
- Применение: машины (стёкла, хром), env-mapped props на улице.
- Объём (типичная городская сцена): **~381 поликвад / 58 батчей** в
  альфа-сорте (до Шага 3.1; сейчас в batch-safe = 1 batch, но те же 381
  поликвад растеризации поверх diffuse'а).
- Текстура: `TEXTURE_page_envmap` (одна на весь движок).

**(2) Cable / overhead wires — `facet.cpp:670 cable_draw`**
- Триггер: рендер facet'а типа cable.
- Что делает: каждый сегмент кабеля сабмитит **2 quads**: `POLY_PAGE_COLOUR`
  с `colour=0` (чёрная тень, `facet.cpp:811`) + `POLY_PAGE_ENVMAP` с
  night-coloured (металлический блеск, `facet.cpp:817`).
- Применение: висящие провода/кабели на уровнях.
- Объём: малый, 0-20 кабелей в сцене → до ~40 polys. ENVMAP submission
  уже частично «съедается» через batch-safe ENVMAP-pre-pass.
- Особенность: использует ту же `POLY_PAGE_ENVMAP` что и пункт (1) →
  оба перенесутся одним фиксом env-map composition в шейдере.

**(3) 2PASS SELF_ILLUM (буква `I` в metadata текстуры)**
- Триггер: автоматический. У страницы текстуры стоит `POLY_PAGE_FLAG_2PASS`
  (установлен при загрузке уровня по букве `I` в metadata текстуры —
  см. `poly_render.cpp:80-85`).
- Что делает: внутри `POLY_add_quad_fast` / `POLY_add_triangle_fast` /
  `POLY_add_nearclipped_triangle` — после первого submit'а на `page`,
  `goto second_page` → автоматический повторный submit того же поли на
  `page + 1`. Overlay-страница (`page+1`) имеет
  `POLY_PAGE_FLAG_TRANSPARENT | POLY_PAGE_FLAG_SELF_ILLUM` → попадает в
  альфа-сорт. См. `poly.cpp:1046, 1123, 1245`.
- Применение: **окна зданий с подсветкой, неоновые вывески, brake-lights
  машин, фары** — всё что светится в темноте.
- Объём: НЕИЗВЕСТЕН — требует runtime-проверки. **Симптомы (`page#853`,
  `#857`, `#852`, `#856` в топ-5 пользователя 2026-05-21) указывают что
  это значимая часть оставшегося «фонового шума»** в альфа-сорте после
  Шага 3.1.
- Текстура overlay'я: per-pair (каждая базовая текстура имеет свою overlay
  на `page+1`), НЕ общая.

**(4) 2PASS WINDOW (буква `D` в metadata текстуры)**
- Триггер: автоматический. Буква `D` в metadata текстуры → `page` получает
  `POLY_PAGE_FLAG_2PASS | POLY_PAGE_FLAG_WINDOW`, `page+1` получает
  `POLY_PAGE_FLAG_WINDOW_2ND`. См. `poly_render.cpp:90-94`.
- Что делает: тот же `goto second_page` механизм что и (3) — отличие
  только в blend mode overlay'я (alpha-blend вместо additive).
- Применение: стёкла зданий с просвечивающим overlay.
- Объём (runtime-замер 2026-05-21 на городской сцене): **0 polys**. На
  этом уровне WINDOW-метка не используется. Может встретиться на других
  уровнях — нужно перепроверить. Но даже если 0 — поддержка в шейдере
  стоит копейки (тот же overlay-canvas, другой `overlay_mode`), делается
  заодно с SELF_ILLUM.

**Runtime-замеры на городской сцене (2026-05-21):**

| Источник | Polys / frame | Решение |
|---|---|---|
| (1) ENVMAP | ~381 (известно из top-5) | Делать. Большой win. |
| (2) Cable | ~40 (estimate) | Заодно с (1) — та же страница. |
| (3) SELF_ILLUM 2PASS | **736** (диагностика) | Делать. Самый большой объём. |
| (4) WINDOW 2PASS | **0** (диагностика) | Поддержать в шейдере, объём ноль |
| **Итого** | **~1157 поликвадов** убираются из второго прохода | |

##### Подход к реализации (один шейдер, четыре источника)

1. **Найти opaque-mesh шейдер.** Скорее всего `tl_vert.glsl` +
   `common_frag.glsl` (используется для всех POLY-quads). Расширить
   его поддержкой multi-texture композиции (env-map + overlay).
2. **Расширить vertex format.** Текущий `GEVertexTL` имеет одну пару UV.
   Опции:
   - Расширить `GEVertexTL` второй парой UV (`env_u/env_v`) + флаги
     композиции (1 байт). Влияет на ВСЕ POLY-вершины (мир/UI/текст) —
     bloat памяти.
   - Отдельный vertex format / pipeline для multi-pass-капабельных
     pages. Меньше bloat, больше кода.
   - *Решение:* выбрать по ходу разработки.
3. **Передать в шейдер дополнительные текстуры:**
   - Sampler `u_envmap_tex` (общая `TEXTURE_page_envmap`).
   - Sampler `u_overlay_tex` (per-page-pair `page+1`'s текстура для
     SELF_ILLUM / WINDOW). Нужен механизм per-page binding (или
     универсальный texture-array подход).
4. **Логика композиции в фрагменте:**
   ```glsl
   vec3 diffuse = texture(u_diffuse_tex, v_uv).rgb;
   if (v_envmap_strength > 0) {
       diffuse *= texture(u_envmap_tex, v_envmap_uv).rgb;
   }
   if (v_overlay_mode != 0) {
       vec4 overlay = texture(u_overlay_tex, v_uv).rgba;
       if (v_overlay_mode == SELF_ILLUM) {
           // additive: emissive overlay
           diffuse += overlay.rgb * overlay.a;
       } else if (v_overlay_mode == WINDOW) {
           // alpha blend: window overlay
           diffuse = mix(diffuse, overlay.rgb, overlay.a);
       }
   }
   out_color = vec4(diffuse, 1.0);
   ```
5. **Перенос вычислений из CPU в шейдер:**
   - ENVMAP sphere-UV: считается из нормали + камеры. Нормаль надо
     прокинуть в vertex format (или подсчитать на CPU как сейчас, но
     записать в `env_u/env_v` слот первого прохода — компромисс пока
     vertex format не расширен per-vertex нормалью).
   - SELF_ILLUM / WINDOW overlay: UV те же что у diffuse (`page+1`
     обычно даёт overlay в тех же координатах). Прокидывается то же
     `v_uv`.
6. **Удалить второй submit:**
   - `mesh.cpp:395-499` — целиком блок ENVMAP re-submit.
   - `facet.cpp:811-817` (cable) — повторный submit, оставить только
     один (соответственно настроив flags в шейдере).
   - `poly.cpp:1046, 1123, 1245` — `goto second_page` пути полностью
     удалить.
7. **Очистка:**
   - `POLY_PAGE_ENVMAP` — больше не используется, удалить из
     `batch_safe_pages[]` (poly.cpp), пометить страницу dead/unused.
   - Overlay-страницы `page+1` для SELF_ILLUM / WINDOW — больше не
     получают submits, их `m_PolyBufUsed` остаётся 0, не попадают
     никуда. Render-state на них больше не нужен.
   - Флаги `POLY_PAGE_FLAG_2PASS`, `_WINDOW`, `_WINDOW_2ND`,
     `_SELF_ILLUM` — частично станут декоративными (информация теперь
     в шейдере). Что удалить, что оставить — по факту.
   - Парсер букв `I`, `D` в `poly_render.cpp:80-94` — теперь должен
     писать per-page metadata («эта страница — self-illum overlay
     с текстурой X» / «window overlay с текстурой Y») вместо
     включения 2PASS-механизма.

##### Гейт визуальной верификации

После каждого подключения подсистемы — обязательная визуальная сверка с
baseline:
- **ENVMAP** — машины со стёклами должны выглядеть как сейчас (с блеском).
  Близкие и дальние, день и ночь, скопления машин.
- **Cable** — провода должны иметь тень + блеск как сейчас.
- **SELF_ILLUM** — подсвеченные окна зданий (ночные сцены особенно),
  неоновые вывески, фары/brake-lights машин — все light-источники
  должны светиться как раньше.
- **WINDOW** — стёкла зданий должны выглядеть как сейчас.

Делать **по фазам, не одним коммитом** — чтобы при глюке знать какая фаза
ломает и легко откатить. Между фазами обязательная сборка + визуальная
проверка.

##### Фазированный план реализации

**Phase A — Scaffolding (готовим скелет, всё работает как было)**
- Расширить vertex format новыми полями: `env_u`, `env_v`,
  `overlay_mode` (uint), и (опционально) `overlay_strength`. Опция —
  расширить `GEVertexTL` или сделать отдельный `GEVertexTLEx` для
  multi-pass капабельных pages.
- Добавить uniform'ы в шейдер: `u_envmap_tex`, `u_overlay_tex`,
  логику композиции в `common_frag.glsl`. Дефолты: `overlay_mode=0`
  (никакого overlay'я), `envmap` не модулирует. То есть **новый код в
  шейдере не активен** пока CPU не записал ненулевые значения.
- Подключить bind env-map текстуры — её id известен глобально
  (`TEXTURE_page_envmap`).
- Подключить механизм bind overlay-текстуры per-batch — это самая
  тонкая часть (см. п. «Тонкое место» ниже).
- **Гейт A:** билд проходит. Визуал идентичен baseline'у (новый код
  существует но никем не активируется — все вершины пишут `overlay_mode=0`).
  Никаких новых батчей, никакого регресса перфа.

**Phase B — ENVMAP миграция**
- В `mesh.cpp:265-393` (первый проход textured-граней) для граней с
  `FACE_FLAG_ENVMAP` дополнительно писать `env_u`/`env_v` (sphere-mapped
  из нормали) и установить `overlay_mode = ENVMAP_BIT` (или флаг
  envmap_strength=1).
- **Удалить** блок `mesh.cpp:395-499` (re-submit ENVMAP) ЦЕЛИКОМ.
- `facet.cpp:670 cable_draw` — оставить ОДИН submit вместо двух. Писать
  env_u/env_v в тот же submit, флаг envmap=1.
- Удалить `POLY_PAGE_ENVMAP` из `batch_safe_pages[]` (poly.cpp).
- **Гейт B:** машины с env-mapped стёклами и хромом выглядят как baseline
  (близкие/дальние, день/ночь, скопления). Кабели — тень+блеск как сейчас.
  Замерить `flush.alpha_polys` — должно НЕ возрасти. ENVMAP батч исчез.

**Phase C — SELF_ILLUM + WINDOW миграция**
- В `POLY_add_quad_fast`/`triangle_fast`/`add_nearclipped_triangle`:
  убрать `goto second_page`. Вместо этого ПРИ ПЕРВОМ submit'е — если
  page имеет `POLY_PAGE_FLAG_2PASS`:
  - Установить `overlay_mode = SELF_ILLUM_BIT` (или `WINDOW_BIT` в
    зависимости от флага page).
  - В per-batch metadata записать какую overlay-текстуру биндить
    (page+1's texture handle). Этот механизм — продукт Phase A.
- Шейдер на frag-этапе сэмплит overlay-текстуру тем же UV (overlay
  использует те же координаты что diffuse в pre-rendered контенте) и
  композитит:
  - `SELF_ILLUM` → additive: `out += overlay.rgb * overlay.a`
  - `WINDOW` → alpha-blend: `out = mix(out, overlay.rgb, overlay.a)`
- **Гейт C:** подсвеченные окна зданий (ночь!), неоновые знаки,
  brake-lights, фары — все light-источники выглядят как baseline.
  Стёкла зданий (если на уровне присутствуют WINDOW-flagged текстуры) —
  выглядят как baseline. Замерить `flush.2pass_polys` — должен стать 0
  (или close-to-0, если есть какие-то edge-case'ы).

**Phase D — Cleanup**
- Удалить мёртвые ветки в `POLY_add_*_fast`: `goto second_page` label,
  условный re-submit.
- Удалить per-poly диагностические счётчики `flush.2pass_*` (не нужны
  если механизм удалён, оставить под `#if DEBUG` если хочется).
- Пометить `POLY_PAGE_FLAG_2PASS` / `_WINDOW` / `_WINDOW_2ND` /
  `_SELF_ILLUM` deprecated или удалить если нигде не читаются.
- Парсер букв `I` / `D` в `poly_render.cpp:80-94` — переписать чтобы
  он писал per-page metadata («эта страница имеет overlay X с режимом
  Y») в новую структуру, которую читает Phase A binding logic.
- Удалить `POLY_PAGE_ENVMAP` если уверены (после grep) что нигде больше
  не используется.

##### Тонкое место: binding overlay-текстуры per-batch

В Phase A нужно решить как фрагмент-шейдер узнаёт **какую** overlay-
текстуру семплить. Варианты:
1. **Per-page binding.** Каждый PolyPage с overlay-материалом знает
   свою overlay texture handle. При `pa->Render()` / `DrawBatchedPolys`
   биндится её overlay перед drawElements. Просто, но **расширяет
   per-draw state change** (надо bind 2 текстуры вместо 1).
2. **Bindless textures.** OpenGL 4.5 фича — текстуры через uvec2
   handle в uniform. Биндить ничего не надо. Но требует extension
   (доступен на современных Linux/Win, не на Steam Deck Mesa-?).
3. **Texture array.** Все overlay-текстуры пакуются в один
   `sampler2DArray`, слой выбирается per-vertex/per-batch индексом.
   Требует чтобы все overlay'и были одного размера (вероятно так —
   все 256×256 или 64×64). Один bind, layer-index per draw.
4. **Atlas overlay'ев.** Все overlay'и в одной большой текстуре,
   UV-offset per page-pair. Похоже на texture array но компактнее по
   памяти.

Решить в Phase A. **Предварительная ставка:** вариант 1 (per-page
binding) — самый простой, ризкорость текстур невелика так как overlay-pages
обычно идут пачкой (один тип здания → один overlay-texture). Если окажется
slow — переключим на texture array (Phase A2).

##### Ожидаемый эффект (по runtime-замерам 2026-05-21)

После всех 4 подсистем в шейдере:
- `POLY_PAGE_ENVMAP` уходит из `batch_safe_pages[]` (1 batch сэкономлен).
- ~**381 ENVMAP-поликвад** больше не растеризуются вторым проходом
  (половина пиксель-фила для env-mapped поверхностей).
- ~**736 SELF_ILLUM overlay-поликвад** перестают сабмититься на page+1
  (это полигоны второго прохода через 2PASS механизм; часть из них
  попадала в opaque-batched draws, часть в альфа-сорт — все исчезают).
- WINDOW: 0 в текущей сцене, но шейдер их поддержит даром.
- **Суммарно ~1100+ поликвадов** убираются из второго прохода.
- В альфа-сорте остаётся «честная» прозрачная геометрия — листва,
  дым, эффекты, partial-alpha decals. Готово для Шага 3.3.

##### Уже сделано (диагностика, оставить до Phase D cleanup)

Диагностические счётчики `flush.2pass_polys` / `flush.2pass_illum_polys`
/ `flush.2pass_window_polys` уже добавлены в `POLY_add_*_fast` (см.
`poly.cpp:1051+`, `:1133+`, `:1260+`). После Phase C они будут показывать
0 (механизм мёртв), в Phase D удаляются вместе с самим механизмом.

Результаты диагностики на городской сцене (2026-05-21):
- `flush.2pass_polys` = **736**
- `flush.2pass_illum_polys` = **736** (все 2PASS — SELF_ILLUM)
- `flush.2pass_window_polys` = **0** (WINDOW в этой сцене не используется)

---

#### Шаг 3.3 — near/far split альфа-сорта (СДЕЛАНО 2026-05-21)

> **Делался вне исходного порядка** (до 3.2) поскольку 3.2 требует
> визуальной супервизии для гейтов (ENVMAP/SELF_ILLUM/WINDOW), а 3.3 —
> локальная и безопасная замена sort-компаратора. Реализован и
> закоммичен.

**Реализация:** lambda-компаратор в `POLY_frame_draw` (poly.cpp). Тюн —
два константа на файловом уровне, прямо перед функцией:
- `POLY_ALPHA_FAR_START_VIEW_Z = 0.35f` — граница в линейном view-z
  (world units, FAR clip = 1.0). 0.35 = последние ~65% видимой глубины
  бакетируются.
- `POLY_ALPHA_FAR_BUCKETS = 128` — число бакетов в FAR-зоне.

См. большой коммент-блок над константами в poly.cpp — там описано
trade-off'ы и как тюнить.

**Реальный эффект:**
- В **городских сценах с малой далёкой alpha** — близкий к нулевому
  (всё попало в NEAR-зону, бакетирования нет).
- В **сценах с плотной далёкой alpha** (деревья, парки, листва на
  расстоянии): **+10% FPS** (79 → 87), **GPU -33%**, **CPU -10-15%**.

**Замечание про взаимодействие с 3.2:** изначально предполагалось что
3.2 делается ПЕРЕД 3.3 и сильно меняет состав альфа-сорта. На практике
3.2 отложен, и 3.3 работает на текущем составе — оптимизация далёкой
честно-прозрачной геометрии (листва, дым, фоновая alpha-декаль). После
3.2 в будущем 3.3 продолжит работать на оставшейся «фоновой» альфе.

**Риск:** граничные случаи на пороге `POLY_ALPHA_FAR_START_VIEW_Z`.
В текущей настройке (0.35, 128 бакетов) — артефакты не видны.
Если будут — тюн соответствующих констант (см. коммент в poly.cpp).

---

#### Шаг 3.4 — opaque-проход (СЛЕДУЮЩЕЕ — РАЗВЕДКА)

> Это **следующий шаг** в работе над `fx_flush`. До этого мы давили на
> альфа-сорт (Шаги 3.1, 3.3) и потенциально multi-pass (3.2 отложен).
> Диагностика 2026-05-21 показала что **основной остаточный кост — в
> opaque-проходе внутри `fx_flush`**.

##### Контекст и текущие цифры (2026-05-21, городская сцена)

`fx_flush`: 83% (0.69ms) ge_* CPU, 25% (0.71ms) GPU, **179 draw-вызовов**.

Разбивка 179 draws:
- `flush.alpha_batches = 62` (альфа-сорт)
- batch-safe (SHADOW/SHADOW_SQ/SHADOW_OVAL/ENVMAP): до 4
- sky: 1
- **~112 draws — opaque-проход** ← основной источник нагрузки

Поликвады: `flush.polys = 7300`, `flush.alpha_polys = 196`.
- Opaque polys ≈ 7300 - 196 = **7104**.
- 7104 / ~112 = **avg 63 polys/draw** — норм, не фрагментация.

То есть opaque batches **сами по себе крупные**, не фрагментированы.
Проблема в **количестве уникальных opaque-страниц** — каждая = state
change + draw. 112 уникальных страниц = 112 переключений на кадр.

##### Что искать (предположения для эксперимента, проверить!)

1. **Сколько реально active opaque pages**, и какие в топе. Возможно
   среди них есть пары/группы с **совместимыми render states** которые
   можно слить в общий батч (или в общий VBO с разными texture binds
   через texture array).
2. **Возможна ли консолидация через 2PASS-overlay'и в шейдер** (тот
   самый отложенный Шаг 3.2): 500-700 из 7104 opaque polys — это
   overlay'и SELF_ILLUM, если их вынести в шейдер, opaque-pages count
   снижается.
3. **Сколько polys у каждой opaque-страницы**: «жирные» (>100 polys)
   vs «худые» (~1-10 polys). Худые — основные кандидаты на слияние
   или другую оптимизацию.
4. **Есть ли страницы с одинаковой текстурой и render-state но разные
   индексы PolyPage** — кандидат на pTheRealPolyPage redirection если
   ещё не сделано.

##### Что РЕАЛЬНО предлагается как первый шаг

**Добавить аналог `flush.alpha_polys` / `flush.alpha_batches` / top-5
overlay'я ДЛЯ OPAQUE-PROHO ДА.** Текущая диагностика уже учитывает
opaque polys в `flush.polys` (общий счётчик), но per-page разбивка
отсутствует.

В poly.cpp в opaque-loop (`for (i = 0; i <= iPolyNumPagesRender; i++)`,
около строки 1737):
- Аналогичный `s_opaque_page_polys[POLY_NUM_PAGES]` массив (как
  существующий `s_alpha_page_polys`).
- Каждый поликвад опейка → инкремент.
- В overlay внизу — топ-5 opaque-страниц с поликаунтом и draws.
- Плюс счётчик `flush.opaque_draws` (= `flush.draws` минус
  `flush.alpha_batches` минус batch-safe drawn — но точнее
  посчитать локально).

После замера на пользовательских сценах **решить**:
- Если топ-5 opaque'а — это 3-5 «толстых» страниц с большой
  геометрией и хорошим батчингом → нечего тут оптимизировать,
  движемся к 3.2 multi-pass или другим направлениям.
- Если топ-5 — это много мелких страниц с мало polys/draw → искать
  возможности для группировки/атласа/multi-pass через шейдер
  (потенциально не обязательно 3.2, может быть другая стратегия).
- Если виден явный «жирный» виновник — оптимизировать его
  персонально (specific optimization).

##### Возможные следующие шаги после 3.4-разведки

- (A) **Atlas-консолидация opaque-текстур.** Объединить много мелких
  текстурных страниц в один атлас → меньше уникальных страниц →
  меньше draws. Большой рефакторинг ассет-pipeline'а.
- (B) **Texture-array path.** Современная альтернатива атласу — все
  текстуры в один `sampler2DArray`, индекс per-vertex/per-batch.
  Меньше bind switching. Требует поддержки уровня OpenGL 3.0+,
  у нас есть.
- (C) **Multi-pass shader port** (Шаг 3.2). Если в opaque топе будут
  именно 2PASS-overlay'и (помечены `[2P]` в overlay), их вынос
  в шейдер сократит ~500-700 polys и несколько draws.
- (D) **Что-то совсем другое** — на основе данных. Возможно
  оптимизация PolyPage state-changes (lazy state setting?),
  возможно instancing для повторяющихся geometries.

**Не выполнять буквально (B/C/D)** — выбирать на основе данных.

##### Подсказки для следующего агента

- В poly.cpp есть существующий `OC_DEBUG_PERF` блок с per-page polys
  / per-page batches для альфа-сорта (с маркером `[2P]` для overlay'ев).
  Скопировать паттерн для opaque — параллельные массивы и переменные.
- Не забыть про `pa->pTheRealPolyPage` redirection — opaque submissions
  могут перенаправляться через ghost-pages. См. `PolyPage::CollectForSort`
  для понимания.
- При экспериментах с opaque — **СОХРАНИТЬ baseline в комментарии**
  (метрики до изменений: 179 draws, 0.69ms ge_*, 0.71ms GPU в типичной
  городской сцене 2026-05-21). Изменения мерять относительно baseline.

---

### Эффекты как класс (НЕ часть Этапа 3 — отдельная история)

Эффекты (`render.scene.fx`, mist/rain/pyro/particles/dirt) — небольшой
вклад в общую нагрузку, но всё-таки тратят CPU на per-frame генерацию
поликвадов через `POLY_add_*`. После завершения Этапа 3 при необходимости
можно отдельно рассмотреть инстансинг / GPU-particle path для этих систем.
Не сейчас.

#### ⭐ CPU-вершинный трансформ листьев / dirt / billboard'ов (отложено, по случаю)

**Контекст (2026-05-21):** замер `render.scene.fx_dirt` фазы — ~3.6% CPU
+ 0.77% GPU в типичном кадре. Внутри `AENG_draw_dirt` каждая летящая
листочка/окурок/мусоринка трансформируется на CPU (per-vertex projection,
билборд-ориентация по камере и т.п.) и сабмитится как готовый поликвад
через `POLY_add_*`. По-современному это должно быть один draw с GPU
billboard'ом (per-instance world-position + scale + rotation как атрибуты,
шейдер строит quad из gl_VertexID или geometry/instancing).

**Почему не сейчас:**
- Стоимость на CPU **умеренная** (3-4%) — не главная боль рядом с
  `fx_flush` (60-80%).
- Перенос требует **отдельной перетряски dirt-системы** — поменять формат
  данных частицы (хранить world-state, не projected-state), новый шейдер,
  новый VBO с persistent layout. Это полноценный рефакторинг подсистемы,
  не локальный фикс.
- На приоритете 1.0 — есть более выгодные цели (Этап 3 рендера).

**Когда делать (звёздочка):**
- **По случаю** — если в рамках какой-то другой задачи всё равно ковыряем
  dirt/particle систему (например меняем спавн листьев, исправляем утечку
  гильз — см. отдельный bug-ticket), может оказаться дешевле заодно
  переписать рендер. Тогда сделать.
- **На Steam Deck-проверке** — если там окажется что dirt даёт >10% CPU
  (на слабом железе пропорция другая), приоритет повышается.
- **После Этапа 3 рендера** — когда `fx_flush` перестанет быть боссом,
  3-4% CPU dirt'а станут более заметны в общей раскладке.

**Кандидаты на тот же класс** (per-frame CPU vertex transform → GPU):
- AENG_draw_rain (дождь — billboards)
- PARTICLE_Draw (общая система частиц)
- AENG_draw_sparks/bangs/fire (пиро)
- MIST grid (сетка тумана 32×32)
- RIBBON_draw (ленты — может требовать особого подхода)

Если будем браться — оптимально сделать **обобщённый GPU-billboard слой**
один раз и переключить на него все эти системы пачкой, а не каждую
по отдельности. Тогда win умножается.

## 5. Риски и подводные камни (найдено в коде)

Этап 1 обязан сохранить поведение 1:1; Этап 2 переносит эту логику в шейдер —
ничего не потерять:

- **Z в clip-space — спец-формула.** `pTLVert.z = 1.0 - POLY_ZCLIP_PLANE / z`
  (polypage.cpp:489) — пометка `BUGFIX-OC-ZBUF-MISMATCH`: должна совпадать с
  inverse-z пространством POLY-пути, иначе z-конфликты персонажей с миром.
- **Near-plane guard:** `if (z < 0.001) z = 0.001` — защита от div/0,
  воспроизвести.
- **Per-vertex освещение персонажей:** half-Lambert `wrap = cos*0.5+0.5` →
  индекс в fade-таблицу; нормали масштабированы `fNormScale = 251`, таблица
  64/128 записей (`MM_pcFadeTable`, `BuildMMLightingTable`). Тонко
  откалибровано (см.
  [stage12_character_lighting_investigation.md](../new_game_planning/stage12_character_lighting_investigation.md)).
- **Per-vertex туман:** считается из view-space Z персонажа
  (`POLY_FADEOUT_START/END`), т.к. в MM-путь приходят уже спроецированные
  вершины — clip-space Z для табличного тумана не годится. В шейдере нужен
  тот же вход.
- **Матрица на вершину:** `bMatIndex` — байт по смещению 12 в вершине;
  индексирует `mm->matrices[]`. В GPU-варианте — атрибут индекса кости.
- **Чётность треугольника в стрипе:** even/odd флип winding
  (polypage.cpp:534) — сохранить порядок индексов.
- **Текстура на болванку:** каждая часть тела рисуется со своей текстурой →
  группировка по текстуре или текстурный атлас/массив (см.
  [stage13_future_ideas.md](../new_game_planning/stage13_future_ideas.md):
  настоящий enabler крупных батчей — текстуры, не шейдер).
- **Прозрачность:** требует сортировки back-to-front — внутри прозрачного
  батча свободно не переупорядочить (opaque — можно, z-буфер).

## 6. Что НЕ делаем

- God-shader как самоцель — шейдер уже один.
- Vulkan — позже, отдельная история (после 1.0, по
  [stage13_future_ideas.md](../new_game_planning/stage13_future_ideas.md)).
- LOD-ы — решено: не добавляем.
- Эффекты в этой итерации — только задел в архитектуре.

## 7. Этап 0 — что проверить перед стартом

- Точные границы фаз `render.scene.things` / `render.scene.fx` (что именно
  попадает в каждую) — по `PERF_PHASE` в
  [aeng.cpp](../new_game/src/engine/graphics/pipeline/aeng.cpp).
- Атрибуция 5838 вызовов: сколько персонажи vs мир vs sky vs farfacet vs fx
  (временный per-путь счётчик `PERF_COUNT`). Подтвердить гипотезу «почти всё
  — персонажи» цифрами, а не предположением.
- Сколько уникальных мешей персонажей/предметов и сколько экземпляров в
  типичной сцене (оценка пользы инстансинга/общего VBO).
- Где именно `figure.cpp` зовёт `ge_draw_multi_matrix` (опаковый
  путь:2165, прозрачный:2486, тени:4155) — все ли через MM, есть ли ещё
  per-poly сабмиттеры в «things»/«fx».

## 8. Открытые вопросы

- Этап 2: автоматический риггинг из иерархии частей тела
  (`body_part_parent_numbers`) — качество весов без художника
  (см. stage13_future_ideas.md, там же workflow с кешем в `openchaos/`).
- Совместимость скелетного пути с тенями персонажей и отражениями в лужах
  (отдельные пути рендера фигуры).
- Формат персистентного буфера: один общий VBO с суб-аллокацией vs VBO на
  уникальный меш.
