# perf_diag — архитектура и план

Встроенный debug-режим диагностики перформанса. Цель: ловить **плавающие**
просадки которые по одному только FPS не видны — резкое изменение в каком-то
из множества per-stage параметров сразу выдаёт где проблема.

## Зачем (мотивация)

Перф просаживается в определённых местах/ситуациях, но без инструментария
непонятно **что именно** тормозит. По общему FPS не видно деградации
отдельной подсистемы (одна стадия может удвоиться по времени, а FPS просесть
чуть-чуть). Нужен режим где видно много per-stage метрик усреднённо + резкие
изменения подсвечиваются цветом, чтобы боковым зрением заметить аномалию.

## Принятые решения

| Вопрос | Решение |
|--------|---------|
| Гейтинг | Compile-time мастер-флаг `OC_DEBUG_PERF` в `debug_config.h`. Off → все макросы замера = `((void)0)`, 0 оверхеда (как `OC_DEBUG_LOG`/dbglog). |
| Файл-лог | Отдельный флаг `OC_DEBUG_PERF_LOG`. Отдельный — чтобы ловить **фазу загрузки с самого старта процесса**, до появления панели/хоткеев. |
| Реестр метрик | Не хардкод полей. Именованные пробы с иерархическим путём (`render.poly.flush`). Имя видится первый раз → слот авто-регистрируется. Добавить метрику = одна строка макроса, без правок в центральном месте. |
| Виды метрик | (1) тайминг — RAII scoped-таймер, за кадр суммируется; (2) счётчик/значение — число (партиклы, МБ, draw calls). |
| Усреднение | Per-metric ring buffer последних N кадров, на экране среднее по окну (тот же механизм что у FPS-оверлея). FPS — одна из метрик. Окно регулируется хоткеем. |
| Подсветка | Цвет = градиент дефолт→красный по относительному отклонению «короткого среднего» от «нормы» (длинного среднего). Peak-hold: резкий всплеск залипает красным ~1–2 сек, чтобы транзиент не промелькнул. |
| Scene-baseline (сравнение со средним по типу сцены) | **Фаза 2.** Сначала только self-relative (отклонение от собственной недавней нормы) — даёт ~90% пользы без ручной калибровки. |
| Расположение панели | Слева, прижата к левому краю (закрывает игровой HUD/радар снизу). Тёмная подложка + текст сверху — точь-в-точь как dbglog, но левая сторона (dbglog держит правую). |
| ⚠️ Сама панель | Отрисовка перф-панели **тоже инструментируется** отдельной метрикой — вдруг панель сама тормозит. |
| Формат файл-лога | CSV, **усреднённо по N кадров** (per-frame может не успевать писаться и тяжело анализировать). Окно усреднения файл-лога регулируемое; перед анализом конкретной проблемы режим усреднения подбирается под неё. |

## Архитектура

Модуль `new_game/src/engine/debug/perf_diag/`:

- `perf_diag.h` — публичный API + макросы. Всё под `#if OC_DEBUG_PERF`;
  иначе макросы → `((void)0)` (аргументы не вычисляются, символы не
  эмитятся). Образец гейтинга — `engine/debug/dbglog/dbglog.h`.
- `perf_diag.cpp` — реестр, ring buffers, усреднение, цвет/аномалии,
  отрисовка панели, файл-логгер. Всё под `#if OC_DEBUG_PERF`.

### API (эскиз)

- `PERF_SCOPE("path.name")` — RAII-зона, мерит wall-clock, суммирует в
  слот за текущий кадр (зона может срабатывать много раз/кадр).
- `PERF_COUNT("path.name", value)` — записать/прибавить числовую метрику.
- `perf_frame_begin()` / `perf_frame_end()` — в главном цикле: перекат
  аккумуляторов в историю, сброс per-frame, total кадра.
- `perf_diag_draw()` — из верхнего UI-слоя (рядом с `DBGLOG_draw`), своя
  `PANEL_start/finish` подложка + `FONT_buffer_add` (фикс-пиксели).
- Хоткеи (только при включённом режиме, геймплей): тогл панели, размер
  окна усреднения, сброс baseline. По образцу `OC_DEBUG_PHYSICS_TIMING`.

### Реестр

Слот метрики: имя, вид (timing/value), per-frame аккумулятор, ring buffer
истории (фикс-размер static, без аллокаций), короткое+длинное средние,
peak-hold таймер. Имя интернится один раз (макрос кэширует указатель на
слот в static local) → за кадр только указатель + сложение. Точечные имена
→ панель группирует/строит дерево по категориям.

### Усреднение и цвет

- Короткое среднее (~30 кадров) = «сейчас», длинное (~неск. секунд) = «норма».
- `deviation = (now - norm) / norm`. Цвет линейно дефолт→красный по
  модулю deviation, с порогом «почти не поменялось → дефолт».
- Peak-hold: при превышении сильного порога метрика латчится красной,
  таймер ~1–2 сек, затем плавный спад цвета.

### Файл-лог (`OC_DEBUG_PERF_LOG`)

CSV рядом с exe (как `crash_log.txt`/`stderr.log`). Колонки = метрики,
строка = усреднение за N кадров. Буферизованная запись. Ловит фазу
загрузки (флаг активен со старта процесса). Окно усреднения настраиваемое.

## Что мерить (грубо → детально, дробить инкрементально)

- **Кадр/цикл:** total, FPS, present/swap, ticks/frame (catch-up физики),
  idle от FPS-кэпа.
- **Физика (per tick, 0..N за кадр):** весь тик, коллизии, PCOM/AI,
  pathfinding, физика машины, projectile/particle sim, EWAY/скрипт.
- **Графика:** весь AENG_draw, culling/build, мир/город, персонажи/figure,
  POLY flush (по page-типам), тени, отражения (wibble),
  bloom/lensflare/postprocess, UI/HUD, composition, текст, **сама
  perf-панель**. Счётчики: draw calls, поли/верты, живые партиклы,
  dlight'ы, texture uploads/кадр.
- **IO/загрузка:** total загрузки уровня + подфазы (map/textures/prims/
  sounds), стриминг на лету, clump-загрузки.
- **Логика:** mission/EWAY, звук, ввод.

Старт грубый (frame total, physics-total, render-total, present,
load-total), углубляем добавлением проб — реестр это позволяет инкрементально.

## План реализации (база, фаза 1)

1. Флаги `OC_DEBUG_PERF` / `OC_DEBUG_PERF_LOG` в `debug_config.h`.
2. Модуль `perf_diag.{h,cpp}` + CMake: реестр, ring buffers, усреднение,
   цвет/peak-hold, API/макросы, no-op ветка.
3. Развязка в главном цикле: `perf_frame_begin/end`; `perf_diag_draw` в
   ui_render рядом с `DBGLOG_draw` (своя метрика на саму панель).
4. Грубые пробы: frame total, physics tick, render (AENG_draw), present,
   level load total.
5. Хоткеи: тогл панели, окно усреднения, сброс baseline.
6. Файл-логгер (`OC_DEBUG_PERF_LOG`), CSV усреднённо.
7. Углубление проб по подсистемам (графика/физика/IO/логика) —
   инкрементально по мере надобности.

Фаза 2 (позже): scene-baseline (снять средние по типу сцены, режим
сравнения, подсветка отклонений от снятого baseline).

## Статус

- 2026-05-17 — дизайн согласован, девлог создан.
- 2026-05-17 — **база реализована** (фаза 1, пп. 1–6 плана). Готово:
  - флаги `OC_DEBUG_PERF` / `OC_DEBUG_PERF_LOG` в `debug_config.h`;
  - модуль `engine/debug/perf_diag/perf_diag.{h,cpp}` + CMake: реестр
    (auto-register по имени), ring buffers, короткое/длинное среднее,
    цвет-градиент по отклонению + peak-hold латч, no-op ветка;
  - врезка в главный цикл (`game.cpp`): `PERF_FRAME_BEGIN/END`, хоткеи
    (KB_4 панель / KB_5 окно усреднения / KB_6 сброс);
  - панель в `ui_render.cpp` рядом с `DBGLOG_draw`, слева, со
    **своим** замером `ui.perf_panel`;
  - грубые пробы: `frame.total`, `frame.fps`, `frame.cap_idle`,
    `physics.total`, `physics.ticks`, `render.draw_screen`,
    `render.overlay`, `render.present`, `io.level_init`,
    `ui.perf_panel`;
  - CSV файл-лог (`perf_log.csv`, усреднённо по N кадров, lazy header
    с пере-эмитом при росте числа метрик), ловит фазу загрузки.
  - Сборка с флагами ON проверена (компилируется, линкуется); флаги
    возвращены в `false`.
- 2026-05-17 — **крупный шрифт панелей**. У bitmap-пути (`FONT_buffer_add`)
  не было масштаба вообще (6-й арг — тень, не scale; глифы фикс. размера,
  общий рендер). Добавлен **аддитивный integer glyph-scale** в общий
  шрифтовый слой (default 1 → весь существующий текст байт-в-байт тот же):
  `FONT_Message.gs`, scale-параметр у `FONT_atlas_draw_glyph` /
  `FONT_draw_coloured_char` / `FONT_string_width`, новый
  `FONT_buffer_add_scaled`, масштабирование advance/tab/line-height/shadow
  во flush-цикле. Перф-панель и dbglog переведены на scaled-путь
  (`PERF_TEXT_SCALE` / `DBGLOG_TEXT_SCALE` = 2; геометрия панелей
  множится на scale). Чанковый upscale (та же атлас-текстура, квад крупнее)
  — то что хотел юзер. Сборка с флагами ON проверена, флаги возвращены
  в `false`. Caveat: при scale 2 панель dbglog ~1260 px шириной — на окне
  уже ~1280 текст уезжает влево; крутить `DBGLOG_TEXT_SCALE`.
- 2026-05-17 — **переработка отображения панели** по фидбеку:
  - убраны миллисекунды отовсюду (показываются только FPS вверху и %
    от кадра у метрик); `frame.fps` как отдельная метрика удалена —
    FPS считается из `frame.total` и пишется в фиксированной шапке;
  - фиксированная шапка вверху: `FPS NNN  avg:Nf` + общая группа
    `frame.*` (general), не в динамическом списке;
  - динамический список сгруппирован по 1-му уровню имени, между
    группами пустая строка, `<group>.total` пиннится внизу своей группы;
  - проценты формат `%05.2f%%` (06.05% / 00.01% / 38.00%);
  - **peak-hold/спайк-механизм удалён** — вместо него цвет **только
    процента** (не всей строки) по нелинейной шкале (pow 0.45)
    бело-серый→красный: ~10% уже заметно, 50 vs 80 различимы, 100% =
    красный;
  - под каждой строкой динамического списка — подчёркивание (тонкий
    `AENG_draw_rect`), чтобы связать далеко разнесённые имя и значение;
  - размер шрифта обеих панелей вынесен в единую кодовую константу
    `DBG_PANEL_TEXT_SCALE` (`engine/debug/debug_panel_text_scale.h`,
    не флаг — не в debug_config.h) — perf и dbglog не
    могут разойтись. Caveat подчёркиваний: рисуются через
    `AENG_draw_rect` (логические коорд.), а текст — литеральные пиксели;
    в in-game катсценах возможен лёгкий вертикальный сдвиг (как у
    backdrop dbglog). В обычном геймплее совпадает.
- 2026-05-17 — мелкие правки по фидбеку: шапка `FPS NNN (N last
  frames)`; ширина перф-панели −25% (240×scale) + колонка значения
  подтянута (160×scale); подчёркивания ярче (`0x80909090`); под
  `*.total` — отдельная **яркая толстая** линия (`0xC8F0F0F0`, ×2
  толщина): сигнал «для общей картины смотри только тотал группы».
  Ширина dbglog возвращена к узкой базе 420×scale (630 раздувало
  панель почти на весь экран при 2×).
- 2026-05-17 — групповые тоталы + меню:
  - **per-group total + сумма = 100%.** Каждая группа получает строку
    `<group> .total`; плюс синтетическая `(other) .total` = 100 − Σ
    групп (немеряные зазоры: input/sfx/gamepad output между блоками).
    Σ всех `.total` = ровно 100% кадра. Правило тотала: явный
    `<group>.total` проба если есть (physics.total — мереный блок),
    иначе сумма «countable»-листьев группы (render =
    draw_screen+overlay+present).
  - **partition-дисциплина:** `is_countable` = KIND_TIME, есть
    group.sub, не `*.total`, sub не с `_`. `render._perf_panel`
    (вложен в render.overlay) помечен `_` → показывается как
    инфо-подметрика, но НЕ в сумме (иначе двойной счёт overlay).
  - Переименования проб: `frame.cap_idle`→`idle.wait` (своя группа
    idle), `ui.perf_panel`→`render._perf_panel`.
  - **главное меню:** цикл `game_attract_mode` (attract.cpp) отдельный
    от game_loop — добавлены `PERF_FRAME_BEGIN/END` + пробы
    `render.frontend` (FRONTEND_loop), `render.present` (AENG_flip),
    `idle.wait`. Панель рисуется тем же post-composition колбэком; в
    меню физики нет → группа physics отсутствует, остальное считается.
- 2026-05-17 — debug-overlay вынесен в mode-independent слой:
  - проблема: текст рисуется в **литеральных пикселях**, а
    прямоугольники (фон/линии) шли через `AENG_draw_rect` =
    логический 640×480 + letterbox/FOV → в меню линии не видны, в
    катсценах уезжают; dbglog маскировал это «overshoot во весь экран».
  - решение: новый примитив `FONT_atlas_fill_rect` (+ `_begin/_end`) —
    залитый прямоугольник тем же pre-transformed TL-путём, что и глифы
    (untextured, alpha-blend, no depth), координаты = реальные
    фреймбуфер-пиксели. Один debug-слой поверх игры, фон+линии в тех же
    координатах что и текст → совпадают в геймплее/меню/катсценах.
  - perf-панель: фон+подчёркивания на нём; dbglog: фон на нём
    (overshoot-костыль и зависимость от POLY-конвейера убраны;
    `AENG_draw_rect`/`PANEL_start` больше не используются этими
    панелями). Цвет текста тотала возвращён к нейтральному (сигнал —
    только яркая линия). Имена членов без префикса группы; ширина
    панели урезана.
- 2026-05-17 — перф-панель: **динамическая раскладка**. Колонка
  значения и ширина панели больше не хардкод — каждый кадр меряются по
  фактическому тексту (`FONT_string_width`): val-col = pad + самое
  длинное имя + маленький зазор; ширина = col + поле значения + pad
  (или ширина шапки, что больше). Панель всегда обтягивает контент,
  «лишнего запаса» нет в принципе. Ширину теперь по сути задаёт длина
  строки-шапки `FPS NNN (Nf last frames)` — это её минимальный предел.
  dbglog подтверждён на новом литерально-пиксельном механизме.
- 2026-05-17 — **БАЗОВАЯ версия панели готова.** Откат самодеятельных
  правок: имена пунктов снова полные (`render.draw_screen`), тоталы —
  те же цвета что обычные строки (отличие только толстая яркая линия),
  без яркого-белого названия. Ширина стабилизирована (замер по
  фиксированному шаблону шапки, без покадрового дёрганья). Зазор
  имя→значение — тюнится `PERF_COL_GAP_PX`. Написана инструкция
  [`panel_guide.md`](panel_guide.md), ссылки на неё добавлены у
  debug-флагов. Файл-лог пользователем не проверялся (логика на месте,
  пишет `perf_log.csv` раз в N кадров).
- 2026-05-17 — **GPU timer queries (вариант D) + счётчик draw-вызовов.**
  Причина: CPU-замеры не видят GPU-стоимость (асинхронна, «всплывает» в
  present) — не было видно, что много строк dbglog роняет FPS.
  Реализовано:
  - бэкенд: «тупой» пул GPU-таймстемпов
    (`backend_opengl/game/gpu_timer.cpp`, `GL_TIMESTAMP`-запросы по
    integer-слотам) + контракт `ge_gpu_timer_*` в
    `game_graphics_engine.h` — вся GL-специфика только в бэкенде;
  - счётчик draw-вызовов: `s_draw_calls` в core.cpp на обоих
    `glDrawElements` (TL + VB-путь), `ge_draw_call_count[/reset]`;
  - perf_diag: `PERF_GPU_SCOPE(name)` (RAII, кольцо `GPU_RINGF=4`
    кадра, асинхронное чтение — CPU не блокируется → профиль не
    искажается); слоты `gpu.<name>`; группа `gpu.*` показывается со
    своим тоталом, но **исключена из суммы 100%** (GPU параллелен
    CPU); подметрики с `_` (`gpu._text_flush`, `gpu._perf_panel`) не
    входят в `gpu .total`; `render.drawcalls` (KIND_VALUE) снимается+
    обнуляется в `frame_end`;
  - врезка: `gpu.draw_screen/overlay/present` (game.cpp),
    `gpu.frontend/present` (attract.cpp), `gpu._perf_panel` +
    `gpu._text_flush` вокруг общего `FONT_buffer_draw` (ui_render.cpp,
    + новый CPU `render._text_flush`).
  - **0 оверхеда при выключенном флаге** — `PERF_GPU_SCOPE` → no-op
    препроцессором (как `PERF_SCOPE`), ge_gpu_timer_* никем не
    вызываются. Сборка с флагами ON проверена (EXIT=0, линкуется).
  - Ограничение: текст одним общим буфером → `_text_flush` не делится
    по панелям; атрибуция — через `render.drawcalls`. Документировано в
    `panel_guide.md` (новый раздел «GPU-замеры»).
- 2026-05-18 — **переделка модели под иерархию «один кусок = одна
  строка, CPU+GPU рядом».** По фидбеку: отдельная `gpu.*` группа и
  `_`-исключения путали; нужна плоская последовательность стадий в
  порядке вызова кадра, обе колонки суммируются в 100%.
  - Slot получил GPU-канал; `PERF_GPU_SCOPE(name)` пишет в **тот же**
    слот что `PERF_SCOPE(name)` (имя без префикса). Отдельная `gpu.*`
    группа убрана.
  - Панель: дерево по dot-пути. Лист печатает себя; контейнер —
    детей + синтетику `<имя>.other = контейнер − Σ(детей)`. Порядок =
    порядок регистрации (= порядок вызова в кадре). Группа 1-го уровня
    разделяется пустой строкой, глубже — нет. Тоталы и жирная линия
    убраны полностью.
  - Две колонки %: `CPU% = строка ÷ CPU-кадр` (Σ=100%);
    `GPU% = строка ÷ полный GPU-кадр` (Σ=100%, отдельный пирог —
    GPU async, его кадр ≠ CPU-кадру). Шапка: `FPS … CPU X.X GPU Y.Y ms`
    (сравнение тоталов = кто боттлнек).
  - Инструментовка переименована по **домену**, не по функциям:
    `physics` (было physics.total), `render` (новая обёртка) →
    `render.scene`/`render.hud` (game.cpp) + `render.post`/`render.ui`/
    `render.present` (разбито внутри `ge_flip`, core.cpp; +include
    perf_diag.h в бэкенд), `render.other` = склейка; `idle`,
    `io.levelload`, `render.frontend` (attract). `present` GPU-метку
    сняли (была вокруг свопа — мутная). `render._perf_panel`/
    `_text_flush` убраны (теперь внутри `render.ui`).
  - Сборка проверена (EXIT=0, линкуется). panel_guide.md переписан под
    новую модель.
- 2026-05-18 — **фикс: present/composition мерялись не на том пути.**
  Симптом: `render.ui` всегда 0, рост строк dbglog тёк в `render.other`.
  Причина: блок composition+post-UI+swap дублируется в **трёх**
  функциях бэкенда, а разбит был только `ge_flip`. Геймплей идёт через
  `AENG_blit → ge_blit_back_buffer` (не `ge_flip`) → сабстадии не
  мерялись, падали в `render.other`. Разбиты идентично все три пути
  кадра: `ge_flip`, `ge_blit_back_buffer` (геймплей),
  `ge_video_draw_and_swap` (FMV; без `render.ui` — там нет UI-колбэка).
  `ge_present_for_drag` намеренно не трогаем (не игровой кадр). Урок:
  при дублированном present-блоке инструментировать ВСЕ копии.
- 2026-05-18 — порядок строк = **реальный порядок вызова в кадре**
  (per-frame seq, штампуется на первом входе скоупа; `s_seq_counter`
  сброс в `frame_begin`). Протухшие слоты (не вызывались > окна
  усреднения) **скрываются** — решает «пустые строки висят после выхода
  в меню» и кривой порядок (present был не в конце из-за порядка
  регистрации). Колонка переименована `CPU+GPU` (честно: wall-clock со
  включённым ожиданием GPU), в шапке `CPU+GPU X.X ms`, добавлена
  строка-заголовок колонок. Также: найден баг — present/composition
  блок дублируется в трёх функциях бэкенда, инструментирован был только
  `ge_flip`; геймплей идёт через `ge_blit_back_buffer` → разбиты все
  три пути (+`ge_video_draw_and_swap`); `ge_present_for_drag` намеренно
  пропущен. Сборки EXIT=0.
- 2026-05-18 — **фикс GPU-замера: убран с контейнеров и свопа.**
  Симптом: `GPU ms` в шапке ≈ или больше `CPU+GPU ms` при явном
  CPU-bound (physics-burn, 25 FPS). Причина: `GL_TIMESTAMP`-дельта на
  скоупе, который включает своп/CPU-простой (`render` обёртка,
  `render.present`), меряет elapsed по таймлайну GPU **включая простой
  и ожидание vsync**, а не GPU-busy → раздувается до ≈ кадра. Фикс:
  GPU-скоупы только на реальных стадиях сабмита
  (`render.scene/hud/post`, `render.ui.{dbglog,perfpanel,textflush}`,
  `render.frontend`); сняты с `render`, `render.ui`, `render.present`
  (все пути ge_flip/ge_blit_back_buffer/ge_video_draw_and_swap).
  `gpu_total` = Σ листовых has_gpu (без контейнерного двойного счёта).
  Теперь `GPU` = реальная работа видяхи, при CPU-bound корректно мал.
  + добавлена экранная легенда хоткеев под шапкой; фикс дёрганья
  ширины на key 5 (шаблон ширины с константными цифрами).
- 2026-05-18 — **убраны обёрточные скоупы (`render`, `render.ui`).**
  По требованию: никаких контейнеров/`.other`-по-уровням — только
  конкретные блоки + единый глобальный `(other)` = кадр − Σ(замеров).
  Точки в имени — лишь визуальная группировка. Панельная логика
  (контейнер/`.other`/рекурсия) сохранена, но при отсутствии обёрток
  вырождается в плоский список (все слоты — корни, `.other` не
  синтезируется). Следствие: GPU/CPU консоли-меню и склейки рендера
  (AENG_blit и пр.) теперь в общем `(other)`, не отдельными строками —
  хочешь видеть → добавляется конкретным замером, без обёрток. Сборка
  EXIT=0, panel_guide переписан под плоскую модель.
- 2026-05-18 — **единый принцип + системное вырезание свопа.**
  Обёртки разрешены, но `.other = обёртка − Σ детей` считается
  одинаково для CPU **и GPU** (раньше GPU у `.other` не было — спец-
  случай, т.к. с обёртки снимали GPU из-за свопа). Решение: служебный
  GPU-скоуп `_swap` (бэкенд, вокруг `present_and_swap`, 3 пути; скрыт,
  не суммируется) + в `GpuScopeTimer` любой замер автоматически вычитает
  пересечение своего интервала со свопом того же кадра (старые
  таймстемпы свопа ещё в backend-слотах — `_swap` ctor идёт после всех
  render-скоупов). → обёртки `render`/`render.ui` снова GPU-мерятся,
  число честное (реальная работа), `render.other`/`render.ui.other`
  имеют и CPU и GPU. Возвращены обёртки в game.cpp/core.cpp. Сборка
  EXIT=0. panel_guide переписан.
- 2026-05-18 — **один макрос меряет CPU+GPU; каждая строка всегда с
  обеими колонками.** `PERF_SCOPE` теперь сам открывает и CPU-, и
  GPU-таймер (тот же слот). `PERF_GPU_SCOPE` удалён (no-op заглушка для
  совместимости), все его вызовы вычищены. У любой стадии всегда обе
  колонки (GPU `00.00%` где видяхе ничего не шлётся: physics/idle/
  present/(other)). Свопом для системного вычитания назначен
  `render.present` по имени (отдельный `_swap` убран); вычитание
  применяется ко всем включая саму present (→ её GPU ≈0). Сборка
  EXIT=0. panel_guide обновлён.
- 2026-05-18 — счётчики (`KIND_VALUE`: `physics.ticks`,
  `render.drawcalls`) вынесены **отдельным списком** под таблицей стадий
  (заголовок `counters`, имя + сырое значение, без %/CPU/GPU). Из
  emit_node убрана inline-печать счётчиков; основная таблица — только
  стадии времени. Сборка EXIT=0.
- 2026-05-18 — UI: убраны пустые строки между группами 1-го уровня в
  основной таблице (уровней мало, мешало и сливало секцию счётчиков);
  у таблицы стадий добавлен левый заголовок `stages` (по аналогии с
  `counters`); зазор перед `counters` обратно 1 строка. Сборка EXIT=0.
- 2026-05-18 — **3 колонки `CPU | ge_* | GPU`.** `CPU` = чистый
  игровой CPU = wall − `ge_*`; `ge_*` = CPU-время внутри графического
  API (сабмит/драйвер + ожидание GPU/свопа); `GPU` = реальный GPU-busy.
  Реализация: `PERF_GE_CALL()` (RAII, depth-counted, без двойного счёта
  вложенных) в горячих `ge_*`: `ge_draw_indexed_primitive[_lit/_vb]`,
  `composition_blit`, `present_and_swap`. Глобальный аккумулятор
  ticks; `ScopeTimer` снимает его на входе/выходе → `Slot.ge_*` канал
  (ring/avg как gpu). `CPU+ge_*` по строкам + `(other)` = 100% (вместе
  = wall); `GPU` — отдельный пирог. Своп-вычитание GPU прежнее. Объём:
  ~доли мс оверхеда при тысячах draw, под debug-флагом. Скомпилилось
  (линк ждал разблокировки exe). panel_guide переписан.
- 2026-05-18 — шапка: CPU/GPU мс → **доля от их суммы** (вместе 100%,
  формат `05.12%`, краснеет по нагрузке, сегментная отрисовка с
  per-сегмент цветом). Порядок колонок таблицы → `ge_* | CPU | GPU`
  (CPU/GPU рядом). EXIT=0.
- 2026-05-18 — фикс переполнения общего FONT-буфера: `FONT_MAX_MESSAGES`
  256→2048, `FONT_BUFFER_SIZE` 8K→64K (font_globals.h). Полный dbglog
  (~1500-1800 msg/кадр) переполнял буфер → текст после лога (значения
  perf-панели) молча отбрасывался. Уточнено в panel_guide: `CPU`-колонка
  = «CPU вне API», для рендер-стадий включает CPU-подготовку отрисовки
  (не баг колонок) — `ge_*` ловит только сам API.
- 2026-05-18 — фикс раздувания `gpu_total`: считаем по **листовым**
  GPU-замерам (`has_gpu && !has_time_children`), не по обёртке
  (`is_gpu_root` удалён вместе с `find_gpu_slot`). Причина: begin/end
  обёртки `render` поглощает простой GPU, пока CPU занят внутри
  (CPU-bound стадия / synthetic burn) — не своп, не вычитается,
  раздувало шапку `GPU`→весь кадр и схлопывало все реальные `GPU%`
  (textflush 30%→4%). Листья окружают только свои всплески сабмита →
  не раздуты. `render.other`/`.other` GPU остаются в таблице (= тот
  простой, полезный сигнал «GPU ждал CPU»), но не в пирог/шапку. EXIT=0.
- 2026-05-18 — **GPU: таймстемп-механизм полностью удалён, заменён на
  `glFinish`-режим (кнопка 7).** Причина: таймстемп-замер по begin/end
  фундаментально считает простой GPU (когда CPU занят) как работу —
  непатчабельно (свежие свидетельства: synthetic burn раздувал GPU и в
  шапке, и схлопывал реальные строки). Удалено: бэкенд `gpu_timer.cpp`,
  контракт `ge_gpu_timer_*`, `GpuScopeTimer`/`gpu_scope_index`/swap-
  вычитание/кольцо, поле `Slot.has_gpu`. Добавлено: `ge_gpu_finish()`
  (=`glFinish`); в `ScopeTimer` dtor CPU/wall фиксируется до finish, при
  `s_gfinish_on` — `glFinish()` и ожидание = реальный GPU стадии
  (последовательно: предыдущий лист уже слит). Кнопка `7` (KB_7)
  toggle: выкл → GPU-колонка пустая, реалистичный FPS; вкл → честный
  per-stage GPU, FPS синтетически занижен (CPU/GPU сериализованы).
  Шапка: CPU/GPU доли только при вкл, иначе FPS + подсказка. EXIT=0.
- 2026-05-18 — цена glFinish-замера вынесена из `render.other` в
  отдельную строку `glfinish`. Глобальный аккумулятор `s_gfinish_accum`
  (ticks): каждый `ScopeTimer` в dtor вычитает из своего wall finish-
  ожидание, прошедшее внутри него (`s_gfinish_accum - gf0`), и **после**
  замера прибавляет своё finish к аккумулятору (родитель тоже вычтет).
  → CPU всех стадий и `render.other` чистые (без оверхеда замера).
  В `frame_end` (только при `s_gfinish_on`) суммарное finish за кадр
  пишется в builtin-слот `glfinish` (свой level-1, KIND_TIME, входит в
  Σ корней → 100% держится). burn возвращён в `render.post`. glFinish-
  режим по умолчанию ВКЛ. EXIT=0.
- 2026-05-18 — каждая колонка нормирована на СВОЙ тотал → каждая сама
  по себе = 100% (не «складывать две»): `ge_total = Σ ge_* корней`,
  `cpu_total = frame − ge_total` (= Σ всех отображаемых pure-CPU, вкл.
  `(other)`), `gpu_total = Σ листьев`. emit делит на свой тотал; `(other)`
  нормируется на cpu_total. Строка цены замера переименована в
  `gpu meas. cost (glFinish)`. EXIT=0 (линк ждал разблокировки exe).
- 2026-05-18 — колонки переименованы для ясности: `ge_*`→`ge_* CPU`,
  `CPU`→`non-ge_* CPU` (обе — CPU-wall, делёж по «внутри граф-API или
  нет»; смысл одинаков с/без glFinish — он лишь добавляет колонку `GPU`).
  Только заголовки/комментарии, логика не менялась.
- 2026-05-18 — `ge_*` сохранён (сигнал «много граф-вызовов на CPU»);
  откат ошибочной выпилки. Своп убран из `ge_*` (`PERF_GE_CALL` снят с
  `present_and_swap`). Добавлен `pre_swap_gpu_drain()` +
  `PERF_PRE_SWAP_GPU_DRAIN()`: явный `glFinish` прямо перед
  `gl_context_swap` (glFinish-режим, вырезается когда perf off), его
  ожидание → корзина GPU-wait. Не полагаемся на «render.ui последний».
  Ряд переименован `gpu meas. cost (glFinish)` → `GPU wait (ignore)` =
  ВСЁ CPU-side GPU-ожидание (per-stage финиши + pre-swap дренаж),
  вычтено из wall → CPU/ge_*/render.other чистые. EXIT=0.
- 2026-05-18 — фикс наложения заголовков колонок: шаг колонки `colw`
  теперь = max(ширина значения, ширины заголовков `ge_* CPU`/
  `non-ge_* CPU`/`GPU`) — длинные тайтлы больше не залезают в соседнюю
  колонку.
- 2026-05-18 — **формат ячейки `процент% (мс)` + строка `total`.** По
  запросу: каждая ячейка таблицы теперь как FPS в шапке — доля ресурса
  и абсолютные миллисекунды стадии (`emit_cell`: `%05.2f%% (%.2fms)`,
  цвет по нагрузке). Внизу таблицы (после `(other)`, перед `counters`) —
  строка-футер `total`: по каждой колонке `100.00%` + сумма мс колонки
  (`cpu_total`/`ge_total`/`gpu_total`); вся строка ярко-янтарным
  (`PERF_TOTAL_*` = 255/215/70), чтобы читалась как итог. `colw`-шаблон
  расширен до `100.00% (0000.00ms)`. Счётчики не тронуты (сырое число).
  Скомпилилось (линк ждал разблокировки exe). panel_guide обновлён.
- 2026-05-18 — **фикс расхождения шапка↔таблица + место glFinish-
  ожидания.** Баг: `cpu_total = кадр − ge_total` включал строку
  GPU-ожидания → колонка `non-ge_* CPU` (тотал 49.6мс) > шапка CPU
  (39.3мс, считалась `кадр − gpu_total`) — два разных определения CPU.
  По требованию: glFinish-ожидание = **CPU** (поток заблокирован, это
  wall) и **графическая сторона** → перенесено **в колонку `ge_*`**,
  входит в `ge_total`/её 100%, но рисуется **отдельной приглушённой
  строкой** (мягко-янтарный 185/150/80) `GPU wait (ignore, glFinish)`
  между `(other)` и `total`, чтобы сразу мысленно вычесть. Слот
  исключён из `s_ord` (не стадия/корень), `ge_total += gpu_wait_ms`,
  `cpu_total = кадр − ge_total` (= чистый non-ge), `(other)` тоже минус
  wait. Шапка `CPU = ge_total + cpu_total` = ровно сумма двух CPU-
  тоталов таблицы → противоречия нет. Маска мс в ячейках `00.00ms`
  (`%05.2f`). Сборка EXIT=0, линк прошёл. panel_guide обновлён.
- 2026-05-18 — мелочи по фидбеку: слово `ignore` убрано из имени ряда
  → `GPU wait (glFinish)`. Шапка при **выкл** glFinish: CPU/GPU идут
  параллельно и не делятся, число = весь кадр → подписано честно
  `CPU+GPU (NN.NNms)  (key 7 to split CPU/GPU)` (процент не пишем — доля
  от кадра тут всегда 100%, бессмысленна). Скомпилилось
  (линк ждал разблокировки exe). panel_guide обновлён.
- 2026-05-18 — **тоглер glFinish (кнопка 7) убран.** По фидбеку: режим
  вкл/выкл слишком запутывал (что меряется, почему FPS другой). Теперь
  glFinish-замер GPU **всегда включён**, пока perf скомпилён (off-режим,
  ветка шапки `CPU+GPU`, KB_7, `s_gfinish_on`, все `if (s_gfinish_on)`
  удалены — путь безусловный). Цена: FPS на панели всегда синтетически
  занижен (CPU/GPU сериализованы) — осознанно: понятность важнее
  реалистичного FPS в дебаге, реальный FPS смотреть без панели.
  Легенда: `7 measure GPU` убран. Полностью компилируется в ноль когда
  perf off (как было). Сборка EXIT=0, линк прошёл. panel_guide/perf_diag.h
  комментарии обновлены.
- 2026-05-18 — по фидбеку, пакет: (1) **кнопка 6 (reset) убрана
  полностью** — обработчик KB_6, легенда, все комментарии (бесполезна,
  путала). (2) **Шапка → 2 строки:** строка 1 `FPS …`; строка 2 —
  **три доли** `GPU wait / CPU / GPU` (раньше CPU включал GPU-wait;
  теперь wait вытащен в свою секцию, проценты считаются между тремя =
  100%). CPU = `ge_total + cpu_total` (вся реальная работа, без wait).
  hdr_tmpl расширен под длинную 2-ю строку. (3) **`GPU wait (glFinish)`
  перенесён ПОСЛЕ `total`, без процента (только мс), вне всех 100%:**
  `ge_total` = только реальный сабмит (минус `+= gpu_wait_ms`),
  `cpu_total = кадр − ge_total − gpu_wait`, строка рисуется ms-only
  после футера. Цель — проценты отражают только важные операции, цена
  замера видна отдельно и в них не входит. Сборка EXIT=0, линк прошёл.
  panel_guide/perf_diag.h обновлены.
- 2026-05-18 — шапка: `GPU wait` убран из формулы процентов. Теперь %
  **только между CPU и GPU** (`hsum = cpu_only + gpu_total`, gw_p
  удалён); `GPU wait` рисуется ms-only приглушённым янтарным, без %, вне
  сплита — контекст «почему такой FPS», не цель оптимизации. hdr_tmpl
  обновлён. Сборка EXIT=0 (линк ждал разблокировки exe). panel_guide
  обновлён.
- 2026-05-18 — по фидбеку: (1) **строка CPU/GPU/GPU-wait в шапке убрана
  целиком** — шапка теперь только `FPS …` (hdr_tmpl сжат, cpu_only/hsum/
  cpu_p/gpu_p/seg-лямбда удалены). (2) У строки `GPU wait` в таблице
  убраны **скобки** вокруг мс (`%05.2fms`, без `()` — там нет процента).
  (3) **Новая строка-футер `split` ПОСЛЕ `total`** (имя жёлтым, как
  total): по каждой из 3 колонок процент = тотал-мс колонки ÷
  (ge_total+cpu_total+gpu_total); три % слева направо = 100%; проценты
  красно-белые `pct_color` как ячейки. Порядок футера: total → split →
  GPU wait. Сборка EXIT=0 (линк ждал разблокировки exe). panel_guide
  обновлён.
- 2026-05-18 — **TEMP synthetic CPU-burn убран из `render.post`**
  (оба дублированных present-пути в core.cpp). Был для проверки панели;
  больше не нужен. `render.post` теперь = только `composition_blit`.
- 2026-05-18 — **perf-панель вынесена из 100% в собственную overhead-
  строку + отдельная очередь текста.** Раньше стоимость самой панели
  (формирование команд + растеризация текста) была размазана внутри
  `render.ui.textflush` вперемешку с dbglog/игровым текстом и искажала
  проценты игры. Сделано:
  - **font.cpp:** общий deferred-text механизм разделён на 2 очереди.
    Очередь 0 (MAIN) = оригинальные глобалы, поведение байт-в-байт. Очередь
    1 (PERF) — только текст панели, свой маленький буфер (8 КБ/384, против
    64 КБ/2048 у MAIN). `FONT_buffer_select()` переключает цель,
    `FONT_buffer_draw_perf()` сбрасывает PERF отдельно (после MAIN — панель
    поверх). **Гейтинг `#if OC_DEBUG_PERF`:** при выключенном флаге PERF-
    очередь и её буфер не компилируются вовсе (0 статической памяти),
    select → no-op, draw_perf → пусто. font.cpp подключает debug_config.h.
  - **perf_diag:** `PERF_OVERHEAD_SCOPE()` (RAII `OverheadTimer`) —
    оборачивает обе точки панели (queue site `PERF_DRAW`, flush site
    `FONT_buffer_draw_perf` в ui_render.cpp). Механизм — точная калька с
    `GPU wait`: глобальные аккумуляторы `s_overhead_accum` (wall) +
    `s_overhead_ge_accum` (ge_*) + `s_overhead_gpu_accum` (свой glFinish-
    слив); каждый `ScopeTimer` вычитает overhead-дельту из своего wall И
    ge-канала (как для glFinish) → `render.ui.other`/`render.other`/
    проценты чистые. Две точки замера **суммируются** (общий аккумулятор,
    не перезапись). За кадр сумма пишется в builtin-слот `perfpanel`
    (с ge/gpu каналами), исключён из `s_ord` и из `cpu_total`/`(other)`
    как `GPU wait`.
  - **Панель:** `render.ui.perfpanel`/`perfflush` как стадии исчезли;
    вместо них одна приглушённая строка `perfpanel` ПОСЛЕ `split`, перед
    `GPU wait`, вне всех 100%. **Честная разбивка по 3 колонкам** (ge_* =
    граф-вызовы flush'а, non-ge = построение глифов CPU, GPU = свой слив;
    Σ = wall), мс без процентов. `GPU wait` тоже расширен до 3 колонок:
    реальное число в `ge_*` (glFinish = граф-вызов), `non-ge`/`GPU` =
    искусственные `00.00ms` для выравнивания. Сборка ON и OFF проверена
    (EXIT=0). panel_guide.md обновлён.

- **2026-05-18 — детализация `render.scene` (7 фаз) + `PERF_PHASE`.**
  - **Проблема:** `render.scene` = `draw_screen`→`AENG_draw`→
    `AENG_draw_city()` — одна линейная функция ~2800 строк. RAII-блоки
    (`{ PERF_SCOPE ... }`) вокруг фаз рискованны (область видимости
    переменных через границы фаз) и дают шумный диф.
  - **Решение — последовательный фазовый таймер.** Новый мини-API
    `PERF_PHASE("name")` / `PERF_PHASE_END()` (perf_diag.h/.cpp): один
    активный «фаза»-слот, не вложенный. `phase_begin` закрывает
    предыдущую фазу (учёт wall/ge_*/GPU **строка-в-строку как
    `ScopeTimer::~ScopeTimer`**: вычет glFinish-wait и `perfpanel`-
    overhead, свой `ge_gpu_finish()`, возврат слива в `s_gfinish_accum`)
    и открывает новую. Маркеры стоят на родных `BreakTime()` точках
    MuckyFoot — границы фаз чистые (уровень тела функции). Защита от
    повисшей фазы (ранний `return` в F10 `g_farfacet_debug`): `s_phase_
    slot=nullptr` в `frame_begin` — частичное межкадровое время НЕ
    книжится.
  - **Фазы (siblings под обёрткой `render.scene`):** `setup`
    (boxes+видимость), `transform` (ambient+трансформ точек+звёзды),
    `shadows`, `reflect` (отражения+лужи+небо+flush), `world` (skyline+
    геометрия уровня), `things` (prims+facets+объекты), `fx` (oval-тени/
    POW/туман/дождь/частицы/грязь+финальный flush). `render.scene.other`
    автоматически = обёртка − фазы (камера-сетап, NIGHT-teardown,
    `POLY_frame_init`, **весь warehouse-путь** — склады не размечены).
  - Один `glFinish` на переход фазы (как у границы scope — ожидаемо для
    режима панели). Сборка ON проверена (EXIT=0); OFF-ветка
    `((void)0)` корректна по построению. panel_guide.md обновлён.
- **Дальше (по запросу):** углубление проб по подсистемам (физика;
  IO-подфазы; при нужде — warehouse-фазы) — инкрементально, **одной
  строкой `PERF_SCOPE`/`PERF_PHASE`** на кусок.
- **Фаза 2 (позже):** scene-baseline.
